# CXG Platform - Tenants Web

CXG Platform의 테넌트(고객사)용 웹 애플리케이션입니다.

## 개요

50인 이하 중소기업을 위한 AI 기반 통합 업무 솔루션의 클라이언트 인터페이스입니다.
**홍보 사이트**와 **업무 시스템**을 하나의 Next.js 앱에서 Route Groups로 분리하여 구현했습니다.

## 아키텍처

### Route Groups 구조

```
src/app/
├── (marketing)/          # 홍보 사이트 (공개)
│   ├── layout.tsx       → 마케팅 레이아웃
│   └── page.tsx         → / (홈페이지)
│
├── (auth)/              # 인증 (공개)
│   ├── layout.tsx       → 인증 레이아웃
│   ├── login/           → /login
│   └── signup/          → /signup
│
└── (app)/               # 업무 시스템 (보호됨)
    ├── layout.tsx       → 앱 레이아웃 (사이드바)
    └── dashboard/       → /dashboard
```

### URL 매핑

| URL | 그룹 | 설명 | 상태 |
|-----|------|------|------|
| `/` | (marketing) | 홈페이지 | ✅ |
| `/login` | (auth) | 로그인 | ✅ |
| `/signup` | (auth) | 회원가입 | ✅ |
| `/dashboard` | (app) | 대시보드 | ✅ |
| `/crm` | (app) | 고객관리 | 📋 예정 |
| `/finance` | (app) | 재무관리 | 📋 예정 |
| `/inventory` | (app) | 재고관리 | 📋 예정 |

## 주요 기능

### 홍보 사이트 (Marketing)
- ✅ Hero Section - 플랫폼 가치 제안
- ✅ Features Grid - 10가지 주요 기능
- ✅ Stats Section - 주요 지표
- ✅ Key Benefits - 6가지 핵심 혜택
- ✅ Modules Overview - 8개 통합 관리 모듈
- ✅ CTA Section - 무료 체험 유도
- ✅ Navigation Header - 메뉴 및 로그인/회원가입

### 인증 (Authentication)
- ✅ 로그인 페이지 (이메일/비밀번호)
- ✅ 회원가입 페이지 (이름/이메일/회사/비밀번호)
- ✅ 폼 유효성 검사
- ✅ 로딩 상태 처리
- 📋 소셜 로그인 (예정)
- 📋 비밀번호 찾기 (예정)

### 업무 시스템 (App)
- ✅ 대시보드 (통계, 최근 활동, 빠른 작업)
- ✅ 사이드바 네비게이션 (8개 메뉴)
- ✅ 반응형 모바일 메뉴
- ✅ 헤더 (검색, 알림)
- ✅ 사용자 프로필
- 📋 CRM 모듈 (예정)
- 📋 Finance 모듈 (예정)
- 📋 기타 업무 모듈 (예정)

### 보안 (Middleware)
- ✅ 라우트 보호 (인증 확인)
- ✅ 자동 리다이렉트
- 📋 JWT 토큰 관리 (예정)
- 📋 Refresh Token (예정)

## 기술 스택

- **Framework**: Next.js 15 (App Router)
- **UI**: React 19, Tailwind CSS v4
- **Components**: shadcn/ui (Radix UI)
- **State**: Zustand (예정)
- **Data Fetching**: TanStack Query (예정)
- **HTTP**: Axios (예정)
- **Icons**: Lucide React
- **Date**: date-fns
- **Auth**: JWT (예정)

## 개발 환경

### 필수 요구사항
- Node.js 20+
- pnpm 8.15.0+

### 설치 및 실행

```bash
# 의존성 설치
pnpm install

# 개발 서버 실행 (포트 8300)
pnpm dev

# 프로덕션 빌드
pnpm build

# 프로덕션 서버 실행
pnpm start
```

## 프로젝트 구조

```
src/
├── app/                       # Next.js App Router
│   ├── (marketing)/          # 홍보 사이트
│   │   ├── layout.tsx        → 마케팅 레이아웃
│   │   └── page.tsx          → 홈페이지
│   ├── (auth)/               # 인증
│   │   ├── layout.tsx        → 인증 레이아웃
│   │   ├── login/            → 로그인
│   │   └── signup/           → 회원가입
│   ├── (app)/                # 업무 시스템
│   │   ├── layout.tsx        → 앱 레이아웃
│   │   └── dashboard/        → 대시보드
│   ├── layout.tsx            # 루트 레이아웃
│   └── globals.css           # 전역 스타일
├── components/               # React 컴포넌트
│   └── ui/                  # shadcn/ui 컴포넌트
├── lib/                     # 유틸리티 함수
│   └── utils.ts            # 공통 유틸
└── middleware.ts            # 라우트 보호
```

## 환경 변수

`.env.local` 파일을 생성하고 다음 변수를 설정하세요:

```env
NEXT_PUBLIC_API_URL=http://localhost:8100
```

## 개발 가이드

### 새로운 페이지 추가

#### 홍보 페이지 (공개)
```bash
# src/app/(marketing)/features/page.tsx
export default function FeaturesPage() {
  return <div>Features</div>;
}
```

#### 업무 모듈 (보호됨)
```bash
# src/app/(app)/crm/page.tsx
export default function CRMPage() {
  return <div>CRM Module</div>;
}
```

### 컴포넌트 추가

shadcn/ui 컴포넌트 추가:
```bash
npx shadcn@latest add [component-name]
```

### 미들웨어 설정

보호할 라우트 추가:
```typescript
// src/middleware.ts
const protectedRoutes = [
  "/dashboard",
  "/crm",      // 추가
  "/finance",  // 추가
];
```

## 라우팅 전략

### Route Groups의 장점

1. **URL에 그룹명이 나타나지 않음**
   - `(app)/dashboard/page.tsx` → `/dashboard` (not `/app/dashboard`)

2. **독립적인 레이아웃**
   - 각 그룹별로 다른 레이아웃 적용
   - 코드 재사용성 증가

3. **명확한 분리**
   - 마케팅, 인증, 앱 로직 분리
   - 유지보수 용이

4. **쉬운 확장**
   - 필요시 별도 앱으로 분리 가능

## 보안

### 인증 플로우

```
1. 사용자가 /dashboard 접근
   ↓
2. Middleware에서 토큰 확인
   ↓
3. 토큰 없음 → /login?from=/dashboard
   ↓
4. 로그인 성공 → /dashboard로 리다이렉트
```

### TODO: 실제 인증 구현

- [ ] JWT 토큰 저장 (httpOnly cookie)
- [ ] Refresh Token 로직
- [ ] API 클라이언트 인터셉터
- [ ] 자동 토큰 갱신

## 성능 최적화

- Server Components 기본 사용
- Client Components는 필요한 곳만 (`"use client"`)
- 이미지 최적화 (`next/image`)
- Route Groups로 코드 스플리팅
- Tailwind CSS JIT 모드

## 배포

- 개발: http://localhost:8300
- 프로덕션: TBD

## 다음 단계

### Phase 1: 인증 시스템 강화
- [ ] 실제 API 연동
- [ ] JWT 토큰 관리
- [ ] Refresh Token
- [ ] 소셜 로그인

### Phase 2: 업무 모듈 구현
- [ ] CRM (고객관리)
- [ ] Finance (재무관리)
- [ ] Inventory (재고관리)
- [ ] Procurement (구매관리)
- [ ] Sales (판매관리)
- [ ] Workflow (워크플로우)

### Phase 3: 마케팅 페이지 확장
- [ ] /features (기능 소개)
- [ ] /pricing (가격 안내)
- [ ] /contact (문의하기)
- [ ] /about (회사 소개)

## 관련 문서

- [Route Groups 구현 가이드](./ROUTE_GROUPS_IMPLEMENTATION.md)
- [Architecture Decision](../../docs/guides/FRONTEND_ARCHITECTURE_DECISION.md)
- [Backend API](../backend-api/README.md)
- [Manager Web](../manager-web/README.md)

## 라이선스

Proprietary


