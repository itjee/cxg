"use client";

import { Card, CardContent } from "@/components/ui/card";
import { ListPageLayout } from "@/components/layouts/list-page-layout";
import { StatsCards } from "@/components/stats/stats-cards";
import { Zap, Play, Pause, Clock } from "lucide-react";

export default function WorkflowsPage() {
  const stats = [
    {
      title: "전체 워크플로우",
      value: "0",
      description: "등록된 워크플로우",
      icon: <Zap className="h-5 w-5" />,
      color: "primary" as const,
    },
    {
      title: "실행 중",
      value: "0",
      description: "현재 실행 중",
      icon: <Play className="h-5 w-5" />,
      color: "success" as const,
    },
    {
      title: "일시 정지",
      value: "0",
      description: "일시 정지됨",
      icon: <Pause className="h-5 w-5" />,
      color: "warning" as const,
    },
    {
      title: "예약됨",
      value: "0",
      description: "스케줄 대기 중",
      icon: <Clock className="h-5 w-5" />,
      color: "default" as const,
    },
  ];

  return (
    <ListPageLayout
      title="워크플로우"
      description="자동화 워크플로우를 관리합니다"
    >
      <StatsCards cards={stats} columns={4} />
      
      <Card>
        <CardContent className="pt-6">
          <div className="rounded-lg border bg-muted/50 p-8 text-center">
            <p className="text-muted-foreground">워크플로우 관리 기능이 곧 추가됩니다.</p>
          </div>
        </CardContent>
      </Card>
    </ListPageLayout>
  );
}
