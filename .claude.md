# Claude Code 사용 지침서

## 프로젝트 개요
- **프로젝트명**: CXG (Customer Experience Gateway)
- **주요 기술 스택**: Next.js 15.5.5, React, TypeScript, GraphQL, Apollo Client, Zustand, Tailwind CSS
- **구조**: Monorepo (apps: manager-web, tenants-web)

## 핵심 규칙

### 1. 답변 및 설명 언어
- **모든 답변과 설명은 한글로 작성**
- 기술 용어나 코드는 원어 그대로 유지
- 예: "React의 useState 훅을 사용하여 상태를 관리합니다"

### 2. 소스 코드 작성 규칙
- **모든 파일에 한글 설명 주석 작성 필수**
- 파일 상단에 `@file`, `@description` 주석 포함
- 주요 기능/로직에 대한 inline 주석 작성
- 복잡한 로직은 여러 줄의 주석으로 명확하게 설명

#### 주석 작성 템플릿

**TypeScript/React 파일:**
```typescript
/**
 * @file 파일명.tsx
 * @description 이 파일의 목적과 역할에 대한 한글 설명
 *
 * 주요 기능:
 * - 기능 1 설명
 * - 기능 2 설명
 */

// === 상수, 설정값 ===
const FILTER_ITEMS = []; // 필터 항목 설정

// === 타입/인터페이스 ===
interface ComponentProps {
  // 프롭 설명
}

// === 컴포넌트 ===
export function MyComponent() {
  // 로직 설명
}
```

**GraphQL 파일:**
```typescript
/**
 * @file queries.ts
 * @description GraphQL 쿼리 정의 및 설명
 */

// 사용자 목록 조회 쿼리
export const GET_USERS = gql`
  query GetUsers(...) {
    ...
  }
`;
```

**Store 파일:**
```typescript
/**
 * @file users.store.ts
 * @description Zustand 상태 관리 스토어
 *
 * 관리되는 상태:
 * - currentPage: 현재 페이지 번호
 * - itemsPerPage: 페이지당 항목 수
 */

export const useUsersStore = create<UsersStore>((set) => ({
  // 초기 상태
  currentPage: 0,
  itemsPerPage: 20,

  // 액션
  setCurrentPage: (page) => set({ currentPage: page }),
}));
```

### 3. 파일 구조 규칙

#### 컴포넌트 파일 구조
```
src/features/[feature]/components/
  ├── [component-name].tsx        # 메인 컴포넌트
  ├── [component]-header.tsx      # 헤더 서브컴포넌트
  ├── [component]-table.tsx       # 테이블 서브컴포넌트
  ├── [component]-form.tsx        # 폼 서브컴포넌트
  ├── [component]-columns.tsx     # 컬럼 정의
  ├── [component]-filter.tsx      # 필터 래퍼 컴포넌트
  └── index.ts                    # 내보내기 파일
```

#### 훅 파일 구조
```
src/features/[feature]/hooks/
  ├── use-[feature].ts            # 데이터 조회 훅
  ├── use-[feature]-mutation.ts   # 데이터 변경 훅
  └── index.ts
```

#### 스토어 파일 구조
```
src/features/[feature]/stores/
  ├── [feature].store.ts          # Zustand 스토어
  └── index.ts
```

### 4. 코딩 컨벤션

#### 네이밍 규칙
- **컴포넌트**: PascalCase (예: `UsersTable`, `UsersFilter`)
- **함수/변수**: camelCase (예: `handleSearchChange`, `activeFilterCount`)
- **상수**: UPPER_SNAKE_CASE (예: `FILTER_ITEMS`, `DEFAULT_PAGE_SIZE`)
- **타입/인터페이스**: PascalCase (예: `UsersFilterState`, `ApiKeysFilterProps`)
- **파일**: kebab-case (예: `users-filter.tsx`, `use-api-keys.ts`)

#### 타입스크립트 규칙
- 모든 props에 대해 인터페이스 정의
- 함수의 반환 타입 명시
- any 타입 사용 금지
- 제네릭 활용으로 재사용성 증대

#### 리액트/상태 관리 규칙
- 로컬 상태(useState): UI 상태 전용 (폼 입력, 토글, 모달 등)
- Zustand 스토어: 페이지/모듈 전체 상태 (페이지 번호, 정렬, 필터 등)
- Apollo Client: 서버 데이터 캐싱 및 동기화
- useCallback: 이벤트 핸들러에 필수

### 5. 모듈별 패턴

#### Jira 스타일 필터링 패턴 (IDAM 모듈)
```typescript
// 페이지 컴포넌트에서 필터 상태 관리
const [localFilters, setLocalFilters] = useState<FilterState>({
  status: null,
  userType: null,
});

const [appliedFilters, setAppliedFilters] = useState<FilterState>({
  status: null,
  userType: null,
});

// 적용 버튼 클릭 시만 서버 쿼리 업데이트
const handleApplyFilters = () => {
  setAppliedFilters(localFilters);
};

// GraphQL 쿼리는 appliedFilters 사용
const { data } = useQuery({
  variables: {
    search: searchText,
    status: appliedFilters.status || undefined,
  }
});
```

#### 공통 필터 컴포넌트 사용 패턴
```typescript
// 모듈별 래퍼 컴포넌트 (users-filter.tsx)
const filterItems: FilterItemConfig[] = [
  {
    key: "status",
    label: "상태",
    options: [
      { value: "ACTIVE", label: "활성" },
      { value: "INACTIVE", label: "비활성" },
    ],
  },
];

export function UsersFilter(props: UsersFilterProps) {
  return (
    <SearchFilter
      {...props}
      filterItems={filterItems}
      searchPlaceholder="사용자명, 이메일 검색..."
    />
  );
}
```

### 6. 커밋 메시지 규칙

**형식:**
```
타입: 간단한 설명

상세 설명 (필요시)

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```

**타입 예시:**
- `feat:` - 새로운 기능
- `refactor:` - 리팩토링
- `fix:` - 버그 수정
- `docs:` - 문서 작성
- `style:` - 코드 스타일 변경
- `test:` - 테스트 추가

**예시:**
```
refactor: IDAM 필터 컴포넌트를 공통 컴포넌트로 통합

- SearchFilter, SearchFilterPopup 공통 컴포넌트 생성
- 6개 모듈별 필터 래퍼 컴포넌트 생성
- 기존 중복된 필터 컴포넌트 12개 파일 삭제
- 모든 모듈 페이지 업데이트하여 새 래퍼 사용

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```

### 7. 문서화 규칙

#### README 작성
- 각 기능/모듈별 README.md 작성
- 한글로 작성하되, 코드 예시는 원어 유지
- 데이터 흐름 다이어그램 포함 (가능시)

#### JSDoc 주석
```typescript
/**
 * 사용자 필터를 적용하고 데이터를 조회합니다.
 *
 * @param filters - 적용할 필터 객체 { status, userType }
 * @param searchText - 검색 텍스트
 * @returns 필터링된 사용자 목록
 *
 * @example
 * const users = await fetchUsers({
 *   filters: { status: 'ACTIVE' },
 *   searchText: '홍길동'
 * });
 */
export async function fetchUsers(filters, searchText) {
  // 구현
}
```

### 8. 기존 코드 분석 및 개선

각 PR/커밋 전에:
1. 기존 패턴 분석
2. 일관성 확인
3. 필요시 리팩토링
4. 한글 주석 추가/개선

### 9. 자주 묻는 질문 (FAQ)

**Q: 이미 작성된 파일에 한글 주석을 추가해야 하나요?**
A: 네, 새로운 작업 시 해당 파일을 수정하면 한글 주석을 추가해주세요. 대규모 주석 추가는 별도 작업으로 진행할 수 있습니다.

**Q: 영문 주석만 있는 코드를 수정할 때는?**
A: 수정하는 부분에 한글 주석을 추가하고, 전체 파일의 상단 주석은 한글로 업데이트해주세요.

**Q: 세 번째 파티 라이브러리는 주석을 추가해야 하나요?**
A: 아니요. 프로젝트 자체 코드만 한글 주석을 작성하면 됩니다.

## 참고 자료

- [프로젝트 구조](/docs/project-structure.md)
- [IDAM 모듈 가이드](/docs/idam-guide.md)
- [GraphQL 사용 패턴](/docs/graphql-patterns.md)

---

**마지막 업데이트**: 2025-11-17
**작성자**: Claude Code
