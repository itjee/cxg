# CXG 플랫폼 - 온보딩 완전가이드

> **새로운 개발자를 위한 CXG 플랫폼 온보딩 가이드**
> - **빠른 시작**: 10분
> - **전체 설정**: 2시간
> - **읽는 순서**: 위에서부터 순서대로

---

## 목차

1. [CXG 플랫폼이란?](#cxg-플랫폼이란)
2. [시스템 아키텍처 개요](#시스템-아키텍처-개요)
3. [기술 스택](#기술-스택)
4. [사전 요구사항](#사전-요구사항)
5. [빠른 시작 (5분)](#빠른-시작-5분)
6. [상세 설정 가이드](#상세-설정-가이드)
7. [검증 및 테스트](#검증-및-테스트)
8. [일상 개발 워크플로우](#일상-개발-워크플로우)
9. [자주 사용하는 명령어 및 포트](#자주-사용하는-명령어--포트)
10. [문제 해결](#문제-해결)
11. [다음 단계 및 학습 경로](#다음-단계--학습-경로)

---

## CXG 플랫폼이란?

**CXG**는 50명 미만의 중소기업(SME)을 위한 AI 기반 비즈니스 지원 플랫폼입니다.

### 주요 기능
- **통합 비즈니스 관리**: ERP, CRM, SCM, WMS, HRM 통합
- **AI 지원**: LLM 통합으로 스마트한 추천 제공
- **멀티테넌트 SaaS**: 안전한 고객 데이터 분리
- **엔터프라이즈급**: 16개 데이터베이스 스키마, 65개 이상 엔티티, 99.9% 가용성
- **최신 기술**: Python FastAPI 백엔드, Next.js 프론트엔드, PostgreSQL 데이터베이스

### 비즈니스 도메인
플랫폼은 매니저 시스템과 테넌트 시스템에 걸쳐 16개의 주요 비즈니스 모듈을 지원합니다:

**매니저 시스템** (플랫폼 운영)
- TNNT: 테넌트 관리
- IFRA: 인프라 관리
- IDAM: 신원 및 접근 관리
- BILL: 청구 및 구독
- MNTR: 모니터링 및 분석
- AUDT: 감사 및 준수
- 기타...

**테넌트 시스템** (비즈니스 운영)
- ADM: 관리
- PSM: 인사 관리
- SRM: 매출 관리
- IVM: 재고 관리
- LWM: 물류 및 창고
- CSM: 고객 관계
- 기타...

---

## 시스템 아키텍처 개요

### 고수준 아키텍처

```
┌─────────────────────────────────────────────────────┐
│                  MANAGER-WEB                        │
│         (플랫폼 관리자 인터페이스)                  │
│          Next.js + React + TypeScript              │
│          포트: 8200                                │
└──────────────────┬──────────────────────────────────┘
                   │
┌──────────────────┴──────────────────────────────────┐
│         TENANTS-WEB (멀티 인스턴스)                 │
│        (비즈니스 애플리케이션)                      │
│         Next.js + React + TypeScript               │
│          포트: 8300                                │
└──────────────────┬──────────────────────────────────┘
                   │
┌──────────────────┴──────────────────────────────────┐
│           BACKEND API                               │
│         FastAPI + Python 3.11+                     │
│              포트: 8100                            │
└──────────────────┬──────────────────────────────────┘
                   │
        ┌──────────┴──────────┐
        │                     │
    ┌───▼──┐           ┌──────▼────┐
    │MANAGER│           │   TENANTS  │
    │  DB   │           │     DB     │
    │ (PG)  │           │    (PG)    │
    └───────┘           └────────────┘
```

### 이중 데이터베이스 아키텍처

1. **매니저 DB** (중앙 플랫폼)
   - 플랫폼 운영 및 관리
   - 테넌트 메타데이터 및 라이선싱
   - 사용자 인증 및 권한 부여
   - 청구 및 모니터링

2. **테넌트 DB** (분산)
   - 각 테넌트는 격리된 데이터 보유
   - 비즈니스 운영 데이터
   - 테넌트 내 사용자 권한

---

## 기술 스택

### 백엔드
- **언어**: Python 3.11+
- **프레임워크**: FastAPI (현대적이고 빠른 비동기 웹 프레임워크)
- **ORM**: SQLAlchemy + SQLModel
- **데이터베이스**: PostgreSQL 14+
- **캐시**: Redis
- **코드 품질**: Ruff (린팅), mypy (타입 검사)
- **AI/ML**: OpenAI API, LangChain, Pinecone (벡터 DB)

### 프론트엔드
- **프레임워크**: Next.js 15+ (React 메타 프레임워크)
- **언어**: TypeScript 5+ (타입 안전 JavaScript)
- **UI 프레임워크**: shadcn/ui (Tailwind 기반 컴포넌트)
- **스타일**: Tailwind CSS v4 (유틸리티 우선 CSS)
- **상태 관리**: Zustand (글로벌 상태), TanStack Query (서버 상태)
- **HTTP 클라이언트**: axios

### 인프라
- **컨테이너**: Docker
- **오케스트레이션**: Kubernetes (AWS의 ECS/EKS)
- **Infrastructure as Code**: Terraform
- **CI/CD**: GitHub Actions, ArgoCD
- **모니터링**: Prometheus, Grafana
- **로깅**: ELK Stack

### 개발 도구
- **패키지 매니저**: pnpm (프론트엔드), pip/uv (백엔드)
- **Git 워크플로우**: GitHub와 브랜치 보호
- **테스팅**: pytest (백엔드), Jest (프론트엔드)
- **API 문서**: Swagger/OpenAPI

---

## 사전 요구사항

### 시스템 요구사항

**최소 하드웨어**
- CPU: 듀얼코어 프로세서 (4개 이상 권장)
- RAM: 8 GB (16 GB 권장)
- 저장소: 20 GB SSD 여유 공간
- 네트워크: 안정적인 인터넷 연결

**운영 체제**
- macOS 11+
- Linux (Ubuntu 20.04+, Debian 11+)
- Windows 10+ (WSL2 포함)

### 필수 도구

**핵심 개발 도구**
- [ ] Git 2.32+
- [ ] Node.js 18+ (프론트엔드용)
- [ ] Python 3.11+ (백엔드용)
- [ ] Docker & Docker Compose
- [ ] PostgreSQL 14+ (로컬 또는 Docker)
- [ ] Redis 7+ (로컬 또는 Docker)

**IDE 및 확장**
- [ ] VS Code (권장) 또는 동등한 편집기
- [ ] VS Code 확장:
  - Python
  - Pylance
  - Ruff (린팅)
  - mypy (타입 검사)
  - ESLint
  - Prettier
  - Thunder Client / REST Client

**패키지 매니저**
- [ ] pnpm (프론트엔드)
- [ ] uv 또는 pip (백엔드)

### 설치 검증

```bash
# 모든 요구사항 확인
git --version          # 2.32+ 이상이어야 함
node --version        # 18+ 이상이어야 함
npm --version
pnpm --version        # pnpm > 8.0
python --version      # 3.11+ 이상이어야 함
docker --version
docker compose version
```

---

## 빠른 시작 (5분)

### 1. 저장소 클론

```bash
git clone https://github.com/your-org/cxg.git
cd cxg
```

### 2. 환경 설정

```bash
# 환경 템플릿 복사
cp apps/backend-api/.env.example apps/backend-api/.env.local
cp apps/manager-web/.env.example apps/manager-web/.env.local
cp apps/tenants-web/.env.example apps/tenants-web/.env.local

# .env 파일 편집 (데이터베이스, API 키 등)
```

### 3. Docker로 서비스 시작

```bash
# PostgreSQL, Redis 및 기타 서비스 시작
docker compose -f docker-compose.dev.yml up -d

# 서비스 준비될 때까지 대기 (30초)
sleep 30
```

### 4. 백엔드 설정

```bash
cd apps/backend-api

# 가상 환경 생성
python -m venv venv
source venv/bin/activate  # Windows에서: venv\Scripts\activate

# 의존성 설치
pip install -r requirements.txt
# 또는 uv 사용 (더 빠름):
uv sync

# 데이터베이스 마이그레이션 실행
alembic upgrade head

# 백엔드 서버 시작
uvicorn src.main:app --reload --port 8100
```

### 5. 프론트엔드 설정 (새 터미널)

```bash
# 노드 의존성 설치
pnpm install

# manager-web 시작
cd apps/manager-web
pnpm dev  # http://localhost:8200에서 실행됨

# 또는 tenants-web을 다른 터미널에서 시작
cd apps/tenants-web
pnpm dev  # http://localhost:8300에서 실행됨
```

### 6. 검증

```bash
# 백엔드 API가 실행 중인지 확인
curl http://localhost:8100/docs  # Swagger UI 열어야 함

# 프론트엔드 확인
# http://localhost:8200 방문 (manager-web)
# http://localhost:8300 방문 (tenants-web)
```

✅ **모든 시스템이 실행 중입니다!** 개발을 시작할 준비가 완료되었습니다.

---

## 상세 설정 가이드

### 단계 1: Git 구성

```bash
# 자격 증명으로 클론
git clone https://github.com/your-org/cxg.git
cd cxg

# Git 구성 (아직 하지 않은 경우)
git config --global user.name "Your Name"
git config --global user.email "your.email@company.com"

# Git 훅 설정 (선택사항이지만 권장)
git config core.hooksPath .git/hooks
```

### 단계 2: Node.js 및 Python 설정

**macOS (Homebrew 사용)**
```bash
# Node.js 설치
brew install node@18
brew link node@18

# Python 설치
brew install python@3.11
brew link python@3.11

# Docker 설치
brew install docker docker-compose
# 그 다음 응용 프로그램에서 Docker Desktop 시작
```

**Ubuntu/Debian**
```bash
# 패키지 관리자 업데이트
sudo apt update && sudo apt upgrade -y

# Node.js 설치
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# Python 설치
sudo apt install -y python3.11 python3.11-venv python3-pip

# Docker 설치
sudo apt install -y docker.io docker-compose
sudo usermod -aG docker $USER
newgrp docker
```

**Windows with WSL2**
```bash
# WSL2 터미널에서 (Ubuntu) 위의 Ubuntu 단계를 따릅니다
# WSL2가 완전히 설치되어 있는지 확인
wsl --list --verbose
```

### 단계 3: pnpm 설치 (프론트엔드 패키지 관리자)

```bash
# pnpm을 전역으로 설치
npm install -g pnpm@latest

# 확인
pnpm --version  # 버전 8.0+ 이상이어야 함
```

### 단계 4: 프로젝트 의존성

```bash
# 루트 의존성 설치 (필요한 경우)
pnpm install

# 백엔드 의존성 설치
cd apps/backend-api
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
# 또는 빠른 설치를 위해 uv 사용
pip install uv
uv sync
cd ../..

# 프론트엔드 의존성 설치
cd apps/manager-web
pnpm install
cd ../..

cd apps/tenants-web
pnpm install
cd ../..
```

### 단계 5: 데이터베이스 및 서비스

```bash
# Docker로 모든 서비스 시작
docker compose -f docker-compose.dev.yml up -d

# 서비스가 실행 중인지 확인
docker ps
# postgres, redis 및 기타 서비스가 표시되어야 함

# PostgreSQL이 준비될 때까지 대기
sleep 30

# 다른 터미널에서 마이그레이션 실행
cd apps/backend-api
source venv/bin/activate
alembic upgrade head  # 데이터베이스 마이그레이션 적용
```

### 단계 6: 환경 구성

**백엔드 환경** (`apps/backend-api/.env.local`)
```env
# 데이터베이스
DATABASE_URL=postgresql://user:password@localhost:5432/cxg_manager
TENANTS_DATABASE_URL=postgresql://user:password@localhost:5432/cxg_tenants

# Redis
REDIS_URL=redis://localhost:6379

# API 구성
API_TITLE=CXG Backend API
API_VERSION=1.0.0
DEBUG=True

# 외부 서비스
OPENAI_API_KEY=your_key_here
```

**프론트엔드 환경** (두 앱 모두에서 `.env.local`)
```env
NEXT_PUBLIC_API_URL=http://localhost:8100
NEXT_PUBLIC_APP_NAME=CXG Manager
```

### 단계 7: 개발 서비스 시작

**터미널 1 - 백엔드**
```bash
cd apps/backend-api
source venv/bin/activate
uvicorn src.main:app --reload --port 8100
# 출력: http://127.0.0.1:8100에서 Uvicorn 실행
```

**터미널 2 - 매니저 웹**
```bash
cd apps/manager-web
pnpm dev
# 출력: Local: http://localhost:8200
```

**터미널 3 - 테넌트 웹** (선택사항)
```bash
cd apps/tenants-web
pnpm dev
# 출력: Local: http://localhost:8300
```

---

## 검증 및 테스트

### 백엔드 검증

```bash
# 1. API가 실행 중인지 확인
curl http://localhost:8100/docs
# Swagger UI에 대한 HTML을 반환해야 함

# 2. 헬스 체크 엔드포인트 테스트
curl http://localhost:8100/api/v1/health
# 반환: {"status": "ok"}

# 3. 데이터베이스 연결 확인
curl http://localhost:8100/api/v1/manager/health/db
# 데이터베이스 상태를 표시해야 함

# 4. 백엔드 테스트 실행
cd apps/backend-api
pytest tests/
```

### 프론트엔드 검증

```bash
# 1. manager-web이 실행 중인지 확인
curl http://localhost:8200

# 2. tenants-web이 실행 중인지 확인
curl http://localhost:8300

# 3. JavaScript 콘솔에서 오류 확인
# 브라우저에서 http://localhost:8200 열기, DevTools 확인

# 4. 프론트엔드 테스트 실행
cd apps/manager-web
pnpm test
```

### 통합 테스트

```bash
# 1. http://localhost:8200 방문 (매니저 웹)
# 로그인 페이지가 표시되어야 함

# 2. http://localhost:8300 방문 (테넌트 웹)
# 테넌트 애플리케이션이 표시되어야 함

# 3. API 문서 확인
# http://localhost:8100/docs 방문

# 4. 데이터베이스를 관리 도구로 보기
# pgAdmin at http://localhost:5050 (구성된 경우)
```

---

## 일상 개발 워크플로우

### 아침 스탠드업 워크플로우

```bash
# 1. 코드 업데이트
git pull origin main

# 2. 의존성 업데이트 확인
pnpm update  # 프론트엔드
# 그리고
pip install --upgrade -r requirements.txt  # 백엔드

# 3. Docker 서비스가 실행 중인지 확인
docker compose -f docker-compose.dev.yml up -d

# 4. 마이그레이션 실행 (새 것이 있는 경우)
cd apps/backend-api
alembic upgrade head

# 5. 개발 서버 시작
# 터미널 1:
cd apps/backend-api && source venv/bin/activate && uvicorn src.main:app --reload --port 8100

# 터미널 2:
cd apps/manager-web && pnpm dev

# 터미널 3 (필요한 경우):
cd apps/tenants-web && pnpm dev
```

### 기능 개발 워크플로우

```bash
# 1. 기능 브랜치 생성 및 전환
git checkout -b feature/your-feature-name

# 2. 변경사항 만들기
# 백엔드 또는 프론트엔드 (또는 둘 다) 파일 편집

# 3. 커밋 전 코드 품질 확인
# 백엔드:
cd apps/backend-api
ruff check . && mypy src/  # 린팅 및 타입 검사

# 프론트엔드:
cd apps/manager-web
pnpm lint && pnpm type-check

# 4. 코드 자동 포맷
# 백엔드:
cd apps/backend-api
ruff format .

# 프론트엔드:
cd apps/manager-web
pnpm format

# 5. 변경사항 테스트
# 백엔드:
cd apps/backend-api
pytest tests/

# 프론트엔드:
cd apps/manager-web
pnpm test

# 6. 명확한 메시지로 커밋
git add .
git commit -m "feat: description of what you did

- Specific change 1
- Specific change 2
- Specific change 3"

# 7. 푸시 및 PR 생성
git push origin feature/your-feature-name
# 그 다음 GitHub에서 PR 생성
```

### 코드 리뷰 체크리스트

푸시하기 전에 다음 항목 확인:

- [ ] 코드가 올바르게 포맷됨
- [ ] 린팅을 통과 (경고 없음)
- [ ] 타입 검사를 통과
- [ ] 테스트 통과 (새 코드는 테스트 포함)
- [ ] 콘솔 오류/경고 없음
- [ ] 커밋 메시지가 명확하고 규칙을 따름
- [ ] API 키나 시크릿이 커밋되지 않음

---

## 자주 사용하는 명령어 & 포트

### 백엔드 명령어

```bash
# 개발 서버 시작
uvicorn src.main:app --reload --port 8100

# 모든 테스트 실행
pytest tests/

# 특정 테스트 파일 실행
pytest tests/unit/test_auth.py

# 커버리지 포함 실행
pytest --cov=src/ tests/

# 코드 포맷
ruff format .

# 코드 린팅
ruff check . --fix

# 타입 검사
mypy src/

# 데이터베이스 마이그레이션
alembic revision --autogenerate -m "Description"
alembic upgrade head
alembic downgrade -1
```

### 프론트엔드 명령어

```bash
# 개발 시작
pnpm dev

# 프로덕션을 위한 빌드
pnpm build

# 프로덕션 서버 시작
pnpm start

# 테스트 실행
pnpm test

# 코드 포맷
pnpm format

# 코드 린팅
pnpm lint

# 타입 검사
pnpm type-check
```

### Docker 명령어

```bash
# 모든 서비스 시작
docker compose -f docker-compose.dev.yml up -d

# 모든 서비스 중지
docker compose -f docker-compose.dev.yml down

# 로그 보기
docker compose -f docker-compose.dev.yml logs -f

# 모든 데이터 리셋 (주의!)
docker compose -f docker-compose.dev.yml down -v
docker compose -f docker-compose.dev.yml up -d
```

### 중요한 포트

| 서비스 | 포트 | URL |
|--------|------|-----|
| 백엔드 API | 8100 | http://localhost:8100 |
| API 문서 (Swagger) | 8100 | http://localhost:8100/docs |
| 매니저 웹 | 8200 | http://localhost:8200 |
| 테넌트 웹 | 8300 | http://localhost:8300 |
| PostgreSQL | 5432 | localhost:5432 |
| Redis | 6379 | localhost:6379 |
| pgAdmin (선택사항) | 5050 | http://localhost:5050 |

---

## 문제 해결

### 포트가 이미 사용 중인 경우

```bash
# 포트를 사용 중인 프로세스 찾기
lsof -i :8100  # 포트 8100 확인

# 포트를 사용 중인 프로세스 종료
kill -9 <PID>

# 또는 스타트업 명령어에서 포트 변경
uvicorn src.main:app --reload --port 8101
```

### 데이터베이스 연결 오류

```bash
# PostgreSQL이 실행 중인지 확인
docker ps | grep postgres

# .env의 연결 문자열 확인
cat apps/backend-api/.env.local | grep DATABASE_URL

# 연결 테스트
psql -U user -h localhost -d cxg_manager -c "\dt"

# PostgreSQL 재시작
docker compose -f docker-compose.dev.yml restart postgres
```

### Python 의존성 오류

```bash
# 캐시 지우고 재설치
rm -rf apps/backend-api/venv
python -m venv apps/backend-api/venv
source apps/backend-api/venv/bin/activate
pip install -r apps/backend-api/requirements.txt
```

### Node/npm 의존성 오류

```bash
# 캐시 지우고 재설치
rm -rf apps/manager-web/node_modules
rm -rf apps/tenants-web/node_modules
pnpm install
```

### Docker 문제

```bash
# Docker 데몬이 실행 중인지 확인
docker ps

# Docker 로그 보기
docker logs <container_id>

# 이미지 다시 빌드
docker compose -f docker-compose.dev.yml build --no-cache

# 모두 리셋
docker compose -f docker-compose.dev.yml down -v
docker compose -f docker-compose.dev.yml up -d
```

### Git 문제

```bash
# git 상태 확인
git status

# 커밋되지 않은 변경사항 취소
git checkout .

# 마지막 커밋 취소 (변경사항 유지)
git reset --soft HEAD~1

# 커밋 기록 보기
git log --oneline -10
```

---

## 다음 단계 & 학습 경로

### 1주차: 편하게 해보기

**1-2일: 설정 & 탐색**
- [ ] 위의 모든 설정 단계 완료
- [ ] `git log`를 실행하여 커밋 히스토리 이해
- [ ] 프로젝트 구조 문서 읽기
- [ ] http://localhost:8100/docs에서 API 문서 탐색

**3-4일: 백엔드 기초**
- [ ] 읽기: 백엔드 개발 가이드
- [ ] 탐색: `apps/backend-api/src` 디렉토리 구조
- [ ] 생성: 첫 번째 간단한 API 엔드포인트
- [ ] 작성: 엔드포인트에 대한 단위 테스트

**5일: 프론트엔드 기초**
- [ ] 읽기: 프론트엔드 개발 가이드
- [ ] 탐색: `apps/manager-web/app` 디렉토리 구조
- [ ] 생성: 첫 번째 간단한 React 컴포넌트
- [ ] 연결: 백엔드 API에 컴포넌트 연결

### 2-3주차: 기능 개발

**모듈 선택**
- [ ] 비즈니스 모듈 선택 (예: ADM, PSM, SRM)
- [ ] 모듈 문서 읽기
- [ ] CRUD 작업을 통해 목록 페이지 만들기
- [ ] 새 기능에 대한 테스트 작성

**코드 품질**
- [ ] Python을 위해 ruff + mypy 배우기
- [ ] TypeScript를 위해 ESLint + Prettier 배우기
- [ ] 사전 커밋 훅 설정
- [ ] 검토를 위해 첫 번째 PR 제출

### 진행 중인 학습

**필수 문서**
1. 아키텍처 가이드 (시스템 설계 이해)
2. API 명세 (API 설계 이해)
3. 컴포넌트 개발 가이드 (프론트엔드 패턴)
4. 데이터베이스 스키마 참고 (데이터 모델 이해)

**개발할 기술**
- TypeScript 숙달 (엄격 모드, 제네릭)
- Python 비동기 프로그래밍 (FastAPI, asyncio)
- SQL 및 데이터베이스 최적화
- 테스트 전략 (단위, 통합, e2e)
- Git 워크플로우 및 코드 리뷰 프로세스

### 도움말 받기

**자료**
- [ ] Slack/Teams: #engineering 채널에서 질문
- [ ] Issues: 알려진 문제에 대해 GitHub issues 확인
- [ ] 문서: `/docs` 폴더에서 검색
- [ ] 코드 예제: 코드베이스에서 유사 코드 검색
- [ ] 멘토: 할당된 온보딩 멘토에게 문의

**문서 빠른 링크**
- [상세 설정 가이드](./10-DEVELOPMENT-SETUP.md)
- [프로젝트 개요](./01-PROJECT-OVERVIEW.md)
- [백엔드 가이드](../04_api/01_API개발가이드_20241108.md)
- [프론트엔드 가이드](../05_frontend/FRONTEND-DEVELOPMENT-GUIDE.md)
- [데이터베이스 가이드](../03_database/START_HERE.md)

---

## 첫 번째 PR 체크리스트

PR을 제출하기 전에:

- [ ] 코드가 올바르게 포맷됨
- [ ] 린팅을 통과 (경고 없음)
- [ ] 타입 검사를 통과
- [ ] 테스트 통과 (새 코드는 테스트 포함)
- [ ] 하드코딩된 값이나 시크릿이 없음
- [ ] 커밋 메시지가 명확하고 규칙을 따름
- [ ] PR 설명이 무엇과 왜를 설명함
- [ ] 모든 리뷰 의견을 해결함

---

**CXG 플랫폼 팀에 오신 것을 환영합니다! 🚀**

질문이 있으신가요? #engineering 채널에서 물어보거나 멘토에게 문의하세요!
