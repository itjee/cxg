# CXG 플랫폼 - 프론트엔드 개발 완전가이드

> **Next.js와 React를 사용한 프론트엔드 개발 완전 레퍼런스**
>
> - **대상 독자**: 프론트엔드 개발자, UI/UX 개발자
> - **읽는 시간**: 2-3시간
> - **마지막 업데이트**: 2024년 11월 8일

---

## 목차

1. [빠른 시작](#빠른-시작)
2. [개발 원칙](#개발-원칙)
3. [프로젝트 구조](#프로젝트-구조)
4. [Shared Module (공통 코드)](#shared-module-공통-코드)
5. [필수 7개 컴포넌트 패턴](#필수-7개-컴포넌트-패턴)
6. [컴포넌트 개발 상세 가이드](#컴포넌트-개발-상세-가이드)
7. [상태 관리](#상태-관리)
8. [타입 정의 상세 가이드 (DTO 패턴)](#타입-정의-상세-가이드-dto-패턴)
9. [서비스 레이어 (API 클라이언트)](#서비스-레이어-api-클라이언트)
10. [TanStack Query Hook (Query Key Factory 패턴)](#tanstack-query-hook-query-key-factory-패턴)
11. [Zustand Store (UI 상태 분리)](#zustand-store-ui-상태-분리)
12. [에러 처리 전략](#에러-처리-전략)
13. [Feature Index (공개 API 패턴)](#feature-index-공개-api-패턴)
14. [API 통합](#api-통합)
15. [목록 페이지 구성](#목록-페이지-구성)
16. [스타일링 시스템](#스타일링-시스템)
17. [베스트 프랙티스](#베스트-프랙티스)
18. [체크리스트](#체크리스트)

---

## 빠른 시작

### 개발 환경 설정

```bash
cd apps/manager-web
pnpm install
pnpm dev  # http://localhost:8200
```

### 기술 스택

| 계층            | 기술            | 버전   | 목적                    |
| --------------- | --------------- | ------ | ----------------------- |
| 프레임워크      | Next.js         | 15+    | 메타프레임워크, SSR/SSG |
| 언어            | TypeScript      | 5+     | 타입 안전성             |
| 런타임          | React           | 19+    | UI 라이브러리           |
| 스타일          | Tailwind CSS    | v4     | 유틸리티 CSS            |
| UI 컴포넌트     | shadcn/ui       | Latest | 재사용 가능 컴포넌트    |
| 서버 상태       | TanStack Query  | v5     | API 데이터 캐싱         |
| 글로벌 상태     | Zustand         | Latest | UI 상태 관리            |
| HTTP 클라이언트 | axios           | Latest | API 통신                |
| 폼              | React Hook Form | Latest | 폼 상태 관리            |
| 검증            | Zod             | Latest | 스키마 검증             |

---

## 개발 원칙

### 1. Feature-Driven Architecture (필수)

모든 기능은 독립적인 Feature 단위로 개발합니다.

```
features/
└── [domain]/           # 도메인별 feature
    ├── components/     # UI 컴포넌트 (7개 필수 컴포넌트)
    ├── hooks/          # TanStack Query 훅
    ├── services/       # API 클라이언트
    ├── stores/         # Zustand 스토어
    ├── types/          # TypeScript 타입
    └── index.ts        # Public exports
```

### 2. 관심사의 분리 (Separation of Concerns)

```
Page (라우팅)
    ↓
Component (UI 렌더링)
    ↓
Hook (로직 재사용)
    ↓
Service (API 통신)
    ↓
Store (상태 관리)
```

### 3. 일관성 우선 (Consistency First)

- **모든 목록 페이지는 동일한 구조** 유지
- **모든 테이블은 columns + table 분리**
- **7개 필수 컴포넌트 준수** (예외 없음)
- **표준 패턴 적용**

### 4. 타입 안전성 (Type Safety)

```typescript
// ✅ 필수
- TypeScript strict 모드
- any 타입 사용 금지
- 모든 함수에 명시적 타입 지정
- API 응답 타입 정의

// ❌ 금지
function handleClick(e: any) {}  // ❌ any 사용
const data = await fetch(...);  // ❌ 타입 미지정
```

---

## 프로젝트 구조

### 완전한 디렉토리 레이아웃

```
apps/manager-web/
├── src/
│   ├── app/                          # Next.js App Router
│   │   ├── (auth)/                  # 인증 라우트 그룹
│   │   │   ├── login/
│   │   │   │   └── page.tsx
│   │   │   └── register/
│   │   │       └── page.tsx
│   │   │
│   │   └── (main)/                  # 메인 애플리케이션 라우트 그룹
│   │       ├── dashboard/
│   │       │   └── page.tsx
│   │       ├── [schema]/            # 비즈니스 도메인 (tnnt, bill, idam, etc)
│   │       │   ├── [entity]/
│   │       │   │   ├── page.tsx     # 목록 페이지
│   │       │   │   ├── [id]/
│   │       │   │   │   └── page.tsx # 상세/수정 페이지
│   │       │   │   └── create/
│   │       │   │       └── page.tsx # 생성 페이지
│   │       │   └── layout.tsx
│   │       ├── api/                 # Next.js API 라우트
│   │       ├── layout.tsx
│   │       └── page.tsx             # 홈 페이지
│   │
│   ├── components/                  # 공통 컴포넌트
│   │   ├── ui/                     # shadcn/ui 컴포넌트
│   │   │   ├── button.tsx
│   │   │   ├── input.tsx
│   │   │   ├── dialog.tsx
│   │   │   └── ...
│   │   │
│   │   ├── layouts/                # 레이아웃 컴포넌트
│   │   │   ├── list-page-layout.tsx
│   │   │   ├── detail-page-layout.tsx
│   │   │   ├── app-layout.tsx
│   │   │   └── sidebar.tsx
│   │   │
│   │   ├── data-table/             # 데이터 테이블
│   │   │   ├── data-table.tsx
│   │   │   ├── data-table-header.tsx
│   │   │   ├── data-table-row.tsx
│   │   │   └── data-table-pagination.tsx
│   │   │
│   │   ├── filters/                # 필터 컴포넌트
│   │   │   ├── list-filter.tsx
│   │   │   ├── filter-field.tsx
│   │   │   └── filter-presets.tsx
│   │   │
│   │   └── stats/                  # 통계 카드
│   │       ├── stats-cards.tsx
│   │       └── stats-card.tsx
│   │
│   ├── features/                    # Feature 모듈 (도메인별)
│   │   ├── tnnt/                   # 테넌트 도메인
│   │   │   ├── tenants/           # 테넌트 엔티티
│   │   │   │   ├── components/
│   │   │   │   │   ├── tenants-columns.tsx      # 테이블 컬럼 (필수)
│   │   │   │   │   ├── tenants-table.tsx        # 테이블 (필수)
│   │   │   │   │   ├── tenants-edit.tsx         # 수정 모달 (필수)
│   │   │   │   │   ├── tenants-form.tsx         # 폼 (필수)
│   │   │   │   │   ├── tenants-header.tsx       # 헤더 (필수)
│   │   │   │   │   ├── tenants-filters.tsx      # 필터 (필수)
│   │   │   │   │   ├── tenants-stats.tsx        # 통계 (필수)
│   │   │   │   │   └── index.ts
│   │   │   │   │
│   │   │   │   ├── hooks/
│   │   │   │   │   ├── use-tenants.ts           # Query Key Factory 포함
│   │   │   │   │   └── index.ts
│   │   │   │   │
│   │   │   │   ├── services/
│   │   │   │   │   ├── tenants.service.ts       # API 클라이언트 (에러 처리, AbortSignal)
│   │   │   │   │   └── index.ts
│   │   │   │   │
│   │   │   │   ├── stores/
│   │   │   │   │   ├── tenants.store.ts         # UI 상태 분리 (Form, Filter, Pagination)
│   │   │   │   │   └── index.ts
│   │   │   │   │
│   │   │   │   ├── types/
│   │   │   │   │   ├── tenants.types.ts         # Entity, DTO, QueryParams
│   │   │   │   │   └── index.ts
│   │   │   │   │
│   │   │   │   └── index.ts              # Public API re-exports
│   │   │   │
│   │   │   ├── subscriptions/       # 구독 엔티티 (동일한 구조)
│   │   │   ├── tenant-roles/        # 테넌트 역할 (동일한 구조)
│   │   │   ├── tenant-users/        # 테넌트 사용자 (동일한 구조)
│   │   │   └── index.ts             # tnnt 도메인 공개 API
│   │   │
│   │   ├── bill/                   # 청구 도메인 (동일한 구조)
│   │   ├── idam/                   # ID & Access 도메인 (동일한 구조)
│   │   └── ...
│   │
│   ├── shared/                       # ✨ 공통 코드 (Shared Module - 중앙집중식)
│   │   ├── hooks/
│   │   │   ├── ui/
│   │   │   │   ├── use-theme.ts     # ✅ 테마 관리
│   │   │   │   ├── use-toast.ts     # ✅ 토스트 알림
│   │   │   │   └── index.ts
│   │   │   ├── auth/               # 📋 향후 추가
│   │   │   │   └── index.ts
│   │   │   └── index.ts            # Public API (모든 훅 export)
│   │   │
│   │   ├── stores/
│   │   │   ├── theme.store.ts      # ✅ 테마 상태 (Zustand)
│   │   │   └── index.ts            # Public API (모든 스토어 export)
│   │   │
│   │   ├── types/
│   │   │   ├── api/
│   │   │   │   ├── api.types.ts    # ✅ API 타입
│   │   │   │   └── index.ts
│   │   │   ├── domain/
│   │   │   │   ├── common.types.ts # ✅ Domain 타입
│   │   │   │   └── index.ts
│   │   │   └── index.ts            # Public API (모든 타입 export)
│   │   │
│   │   └── index.ts                # ✅ Shared Module 최상위 공개 API
│   │
│   ├── lib/                          # 유틸리티 및 설정
│   │   ├── api.ts                   # Axios 인스턴스
│   │   ├── errors.ts                # ApiError 클래스
│   │   ├── utils.ts                 # 유틸리티 함수
│   │   └── constants.ts             # 상수
│   │
│   ├── providers/                    # Context Providers
│   │   ├── query-provider.tsx
│   │   ├── theme-provider.tsx
│   │   └── index.ts
│   │
│   ├── styles/                       # 전역 스타일
│   │   ├── globals.css
│   │   └── variables.css
│   │
│   └── middleware.ts                 # Next.js 미들웨어
│
├── public/                           # 정적 파일
├── .env.local                        # 환경 변수
├── next.config.ts                    # Next.js 설정
└── package.json
```

---

## Shared Module (공통 코드)

### 개요

**Shared Module**은 모든 기능(Feature)에서 사용할 수 있는 공통 코드를 중앙에서 관리하는 구조입니다.

중앙집중식 관리로 코드 중복을 제거하고, 의존성을 명확하게 하며, 팀 협업을 용이하게 합니다.

```
src/shared/
├── hooks/           # 전역 훅 (UI, Auth 등)
├── stores/          # 전역 상태 (Zustand)
├── types/           # 공통 타입 (API, Domain)
└── index.ts         # Public API (공개 인터페이스)
```

### 실제 구조 (Manager-Web 예시)

```
src/shared/
├── hooks/
│   ├── ui/
│   │   ├── use-theme.ts      ✅ 테마 관리
│   │   ├── use-toast.ts      ✅ 토스트 알림
│   │   └── index.ts          # UI hooks export
│   ├── auth/                 📋 향후 추가
│   └── index.ts              # 최상위 export
│
├── stores/
│   ├── theme.store.ts        ✅ Zustand 스토어
│   └── index.ts              # stores export
│
├── types/
│   ├── api/
│   │   ├── api.types.ts      ✅ API 타입
│   │   └── index.ts          # API types export
│   ├── domain/
│   │   ├── common.types.ts   ✅ Common 타입
│   │   └── index.ts          # Domain types export
│   └── index.ts              # 최상위 export
│
└── index.ts                  # Shared Module 공개 API
```

### 사용 방법

#### 1️⃣ 가장 깔끔한 방식 (권장)

```typescript
// 최상위 export에서 바로 import
import { useTheme, useToast } from "@/shared/hooks";
import { useThemeStore } from "@/shared/stores";
import type { User, ApiError } from "@/shared/types";

// 또는 shared index에서 바로 import
import { useTheme, useThemeStore } from "@/shared";
import type { User, ApiError } from "@/shared";
```

#### 2️⃣ 명시적인 방식

```typescript
// 카테고리별로 명시적으로 import
import { useTheme } from "@/shared/hooks/ui";
import { useThemeStore } from "@/shared/stores";
import type { User } from "@/shared/types/domain";
import type { ApiError } from "@/shared/types/api";
```

#### ❌ 절대 금지

```typescript
// 이전 구조는 더 이상 존재하지 않음!
import { useTheme } from "@/hooks"; // ❌ 404
import { useThemeStore } from "@/stores"; // ❌ 404
import type { User } from "@/types"; // ❌ 404
```

### Shared vs Feature 구분

| 항목          | Shared                            | Feature                                      |
| ------------- | --------------------------------- | -------------------------------------------- |
| **위치**      | `src/shared/`                     | `src/features/[domain]/[entity]/`            |
| **소유**      | 전체 팀                           | 특정 피처 팀                                 |
| **목적**      | 모든 피처가 재사용                | 특정 피처만 사용                             |
| **의존성**    | 독립적 (의존 없음)                | Shared에 의존 가능                           |
| **예시**      | useTheme, useAuthStore, User 타입 | useCreateTenant, TenantForm, useTenantsQuery |
| **변경 영향** | 큼 (모든 피처 영향)               | 작음 (해당 피처만)                           |

### 📂 각 디렉토리 상세 설명

#### Hooks 디렉토리

```
shared/hooks/
├── ui/                    # UI 관련 전역 훅
│   ├── use-theme.ts      # 테마 전환/관리
│   ├── use-toast.ts      # 토스트 알림 (상태 관리)
│   └── index.ts
├── auth/                  # 인증 관련 훅 (향후)
│   └── index.ts
└── index.ts              # 모든 훅 export
```

**언제 Shared hooks에 추가하나?**

- ✅ 여러 피처에서 공통으로 사용하는 로직
- ✅ UI 상태 (테마, 다크모드, 토스트)
- ✅ 전역 인증 로직
- ❌ 특정 피처에만 필요한 로직

#### Stores 디렉토리

```
shared/stores/
├── theme.store.ts        # 테마 상태 (Zustand)
├── auth.store.ts         # 인증 상태 (향후)
└── index.ts             # 모든 스토어 export
```

**언제 Shared stores에 추가하나?**

- ✅ 앱 전체에서 공유하는 상태 (테마, 인증)
- ✅ 여러 페이지/컴포넌트에서 접근하는 상태
- ❌ 특정 피처의 UI 상태 (Feature store 사용)

#### Types 디렉토리

```
shared/types/
├── api/                  # API 관련 공통 타입
│   ├── api.types.ts     # LoginRequest, ApiError 등
│   └── index.ts
├── domain/              # 도메인 공통 타입
│   ├── common.types.ts  # User, Tenant, Role 등
│   └── index.ts
└── index.ts            # 모든 타입 export
```

**API Types 예시:**

```typescript
// api.types.ts
export interface ApiError {}
export interface LoginRequest {}
export interface LoginResponse {}
export interface PaginationParams {}
```

**Domain Types 예시:**

```typescript
// common.types.ts
export interface User {}
export interface Tenant {}
export interface MenuItem {}
export type TenantStatus = "ACTIVE" | "INACTIVE" | "SUSPENDED";
```

### ✅ Best Practices

#### 1. **Public API 원칙** - 항상 index.ts를 통해 export

```typescript
// ✅ 좋음: index.ts를 통한 export
import { useTheme } from "@/shared/hooks";
import { useTheme } from "@/shared/hooks/ui"; // 명시적 OK
import { useTheme } from "@/shared"; // 최상위 OK

// ❌ 나쁨: 직접 import
import { useTheme } from "@/shared/hooks/ui/use-theme";
```

#### 2. **계층적 구조 유지** - 관심사별로 폴더 분리

```
shared/hooks/
├── ui/        # UI 관련 (useTheme, useToast)
├── auth/      # 인증 관련 (useAuth, usePermission)
├── data/      # 데이터 관련 (향후)
└── index.ts

shared/types/
├── api/       # API 타입
├── domain/    # 도메인 타입
└── index.ts
```

#### 3. **선택적 export** - 필요한 것만 export

```typescript
// ✅ 좋음: 필요한 것만 명시적으로 export
// shared/hooks/ui/index.ts
export { useTheme } from "./use-theme";
export { useToast, toast } from "./use-toast";

// ✅ 좋음: 폴더 단위로 export
// shared/hooks/index.ts
export * from "./ui";
export * from "./auth";

// ❌ 나쁨: 전체 export 후 re-export
export * from "./use-theme"; // 세부 구현까지 노출
```

#### 4. **Breaking Change 주의**

Shared 변경 시 모든 피처가 영향을 받으므로:

- 문서화 필수
- 마이그레이션 가이드 작성
- 버전 관리 고려

```typescript
// ❌ Breaking Change 예시
// Before
export interface User {
  id: string;
  name: string;
}

// After: 필드 제거 → 모든 피처 영향
export interface User {
  id: string;
  // name 필드 제거 → 컴파일 에러!
}
```

#### 5. **의존성 구조 명확화**

```
✅ 안전한 의존성 (단방향)
Features → Shared
Features → Features

❌ 금지된 의존성 (순환)
Shared → Features  (절대 금지!)
Features A → Features B → Features A
```

### 폴더 추가 시 체크리스트

새로운 공통 코드를 Shared에 추가할 때:

- [ ] 여러 피처에서 사용되는가? (1개 피처만 사용 → Feature store)
- [ ] 앱 전체에 영향을 미치는 변경인가? (Breaking change 검토)
- [ ] 의존성 구조가 단방향인가?
- [ ] index.ts를 통한 public API가 정의되었는가?
- [ ] 문서가 업데이트되었는가?
- [ ] 타입이 안전한가? (TypeScript strict mode)

---

## 필수 7개 컴포넌트 패턴

### 개요

모든 목록 페이지는 다음 7개의 필수 컴포넌트를 포함해야 합니다.

| #   | 컴포넌트 | 파일명                 | 역할                 | 필수 |
| --- | -------- | ---------------------- | -------------------- | ---- |
| 1   | Columns  | `[domain]-columns.tsx` | 테이블 컬럼 정의     | ✅   |
| 2   | Table    | `[domain]-table.tsx`   | 데이터 테이블 렌더링 | ✅   |
| 3   | Edit     | `[domain]-edit.tsx`    | 수정 모달/페이지     | ✅   |
| 4   | Form     | `[domain]-form.tsx`    | 생성/수정 폼         | ✅   |
| 5   | Header   | `[domain]-header.tsx`  | 페이지 헤더          | ✅   |
| 6   | Filters  | `[domain]-filters.tsx` | 검색/필터 UI         | ✅   |
| 7   | Stats    | `[domain]-stats.tsx`   | 통계 카드            | ✅   |

### 목록 페이지 구조

```
┌──────────────────────────────────┐
│ Header + Actions                 │  invoice-header.tsx
├──────────────────────────────────┤
│ Stats Cards                       │  invoice-stats.tsx
├──────────────────────────────────┤
│ Filters & Search                 │  invoice-filters.tsx
├──────────────────────────────────┤
│ Data Table + Pagination          │  invoice-table.tsx + columns
├──────────────────────────────────┤
│ Edit Modal (Overlay)             │  invoice-edit.tsx
└──────────────────────────────────┘
```

---

## 컴포넌트 개발 상세 가이드

### 1. Columns 컴포넌트 (테이블 컬럼 정의)

**역할:**

- TanStack Table 컬럼 정의
- 포맷 함수 (날짜, 통화, 상태 등)
- 상수 (색상 매핑, 라벨)
- 액션 핸들러 타입

**구현 예시:**

```typescript
// features/invoice/components/invoice-columns.tsx
import { ColumnDef } from "@tanstack/react-table";
import { DataTableColumnHeader } from "@/components/ui/data-table";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import type { Invoice, InvoiceStatus } from "../types";

// 포맷 함수
const formatCurrency = (amount: number) => {
  return new Intl.NumberFormat("ko-KR", {
    style: "currency",
    currency: "KRW",
  }).format(amount);
};

const formatDate = (date: string) => {
  return new Date(date).toLocaleDateString("ko-KR");
};

// 상수 정의
const statusColors: Record<InvoiceStatus, string> = {
  PAID: "bg-green-100 text-green-800",
  PENDING: "bg-yellow-100 text-yellow-800",
  OVERDUE: "bg-red-100 text-red-800",
  CANCELLED: "bg-gray-100 text-gray-800",
};

const statusLabels: Record<InvoiceStatus, string> = {
  PAID: "지급 완료",
  PENDING: "미지급",
  OVERDUE: "연체",
  CANCELLED: "취소",
};

// 인터페이스
interface GetColumnsParams {
  onViewDetails?: (invoice: Invoice) => void;
  onDownload?: (invoice: Invoice) => void;
  onEdit?: (invoice: Invoice) => void;
  onDelete?: (invoice: Invoice) => void;
}

// 컬럼 생성 함수
export const getInvoiceColumns = ({
  onViewDetails,
  onDownload,
  onEdit,
  onDelete,
}: GetColumnsParams = {}): ColumnDef<Invoice>[] => [
  // NO 컬럼 (행 번호)
  {
    id: "rowNumber",
    header: () => <div className="text-center">NO</div>,
    cell: ({ row, table }) => {
      const { pageIndex, pageSize } = table.getState().pagination;
      return (
        <div className="text-center">
          {pageIndex * pageSize + row.index + 1}
        </div>
      );
    },
    enableSorting: false,
    enableHiding: false,
  },

  // 청구서 번호
  {
    accessorKey: "invoiceNumber",
    header: ({ column }) => (
      <DataTableColumnHeader column={column} title="청구서 번호" />
    ),
    cell: ({ row }) => (
      <code className="text-xs bg-muted px-2 py-1 rounded">
        {row.getValue("invoiceNumber")}
      </code>
    ),
  },

  // 테넌트명
  {
    accessorKey: "tenantName",
    header: ({ column }) => (
      <DataTableColumnHeader column={column} title="테넌트" />
    ),
    cell: ({ row }) => row.getValue("tenantName"),
  },

  // 금액
  {
    accessorKey: "amount",
    header: ({ column }) => (
      <DataTableColumnHeader column={column} title="금액" />
    ),
    cell: ({ row }) => formatCurrency(row.getValue("amount")),
    meta: {
      align: "right",
    },
  },

  // 상태
  {
    accessorKey: "status",
    header: ({ column }) => (
      <DataTableColumnHeader column={column} title="상태" />
    ),
    cell: ({ row }) => {
      const status = row.getValue("status") as InvoiceStatus;
      return (
        <Badge className={statusColors[status]}>{statusLabels[status]}</Badge>
      );
    },
  },

  // 발급일
  {
    accessorKey: "issueDate",
    header: ({ column }) => (
      <DataTableColumnHeader column={column} title="발급일" />
    ),
    cell: ({ row }) => formatDate(row.getValue("issueDate")),
  },

  // 마감일
  {
    accessorKey: "dueDate",
    header: ({ column }) => (
      <DataTableColumnHeader column={column} title="마감일" />
    ),
    cell: ({ row }) => formatDate(row.getValue("dueDate")),
  },

  // 액션
  {
    id: "actions",
    header: () => <div className="text-right">작업</div>,
    cell: ({ row }) => (
      <div className="flex justify-end gap-2">
        <Button
          size="sm"
          variant="outline"
          onClick={() => onViewDetails?.(row.original)}
        >
          상세
        </Button>
        <Button
          size="sm"
          variant="outline"
          onClick={() => onEdit?.(row.original)}
        >
          수정
        </Button>
        <Button
          size="sm"
          variant="destructive"
          onClick={() => onDelete?.(row.original)}
        >
          삭제
        </Button>
      </div>
    ),
    enableSorting: false,
    enableHiding: false,
  },
];
```

### 2. Table 컴포넌트 (데이터 테이블 렌더링)

**역할:**

- columns 파일에서 컬럼 정의 import
- DataTable 컴포넌트 설정
- Zustand 스토어 연동
- 페이지네이션/정렬 설정

**구현 예시:**

```typescript
// features/invoice/components/invoice-table.tsx
"use client";

import { DataTable } from "@/components/ui/data-table";
import { useInvoiceStore } from "../stores";
import { getInvoiceColumns } from "./invoice-columns";
import type { Invoice } from "../types";

interface InvoiceTableProps {
  data: Invoice[];
  total: number;
  onViewDetails?: (invoice: Invoice) => void;
  onEdit?: (invoice: Invoice) => void;
  onDelete?: (invoice: Invoice) => void;
  isLoading?: boolean;
}

export function InvoiceTable({
  data,
  total,
  onViewDetails,
  onEdit,
  onDelete,
  isLoading,
}: InvoiceTableProps) {
  const {
    sorting,
    setSorting,
    currentPage,
    setCurrentPage,
    itemsPerPage,
    setItemsPerPage,
  } = useInvoiceStore();

  const columns = getInvoiceColumns({
    onViewDetails,
    onEdit,
    onDelete,
  });

  const pageCount = Math.ceil(total / itemsPerPage);

  return (
    <DataTable
      columns={columns}
      data={data}
      sorting={sorting}
      onSortingChange={setSorting}
      pagination={{
        pageIndex: currentPage,
        pageSize: itemsPerPage,
        totalPages: pageCount,
        totalItems: total,
      }}
      onPaginationChange={(newPagination) => {
        if (newPagination.pageIndex !== undefined) {
          setCurrentPage(newPagination.pageIndex);
        }
        if (newPagination.pageSize !== undefined) {
          setItemsPerPage(newPagination.pageSize);
        }
      }}
      loading={isLoading}
      emptyMessage="등록된 청구서가 없습니다"
    />
  );
}
```

### 3. Edit 컴포넌트 (수정 모달)

**역할:**

- 수정 UI (모달 또는 페이지)
- form 컴포넌트 통합
- mutation 호출
- 성공/실패 처리

**구현 예시:**

```typescript
// features/invoice/components/invoice-edit.tsx
"use client";

import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog";
import { toast } from "sonner";
import { useInvoiceStore } from "../stores";
import { useInvoice, useUpdateInvoice } from "../hooks";
import { InvoiceForm } from "./invoice-form";

export function InvoiceEdit() {
  const { editingId, closeForm } = useInvoiceStore();
  const { data: invoice, isLoading } = useInvoice(editingId!);
  const { mutate: updateInvoice, isPending } = useUpdateInvoice();

  if (!editingId) return null;

  const handleSubmit = (data: any) => {
    updateInvoice(
      { id: editingId, data },
      {
        onSuccess: () => {
          toast.success("청구서가 수정되었습니다");
          closeForm();
        },
        onError: (error) => {
          toast.error(error.message || "수정 실패");
        },
      }
    );
  };

  return (
    <Dialog open={!!editingId} onOpenChange={closeForm}>
      <DialogContent className="sm:max-w-[600px]">
        <DialogHeader>
          <DialogTitle>청구서 수정</DialogTitle>
        </DialogHeader>
        {isLoading ? (
          <div className="py-8 text-center">로딩 중...</div>
        ) : (
          <InvoiceForm
            initialData={invoice}
            onSubmit={handleSubmit}
            onCancel={closeForm}
            isSubmitting={isPending}
          />
        )}
      </DialogContent>
    </Dialog>
  );
}
```

### 4. Form 컴포넌트 (생성/수정 폼)

**역할:**

- React Hook Form + Zod 통합
- 폼 필드 정의
- Validation
- 제출 핸들러

**구현 예시:**

```typescript
// features/invoice/components/invoice-form.tsx
"use client";

import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { z } from "zod";
import {
  Form,
  FormField,
  FormItem,
  FormLabel,
  FormControl,
  FormMessage,
} from "@/components/ui/form";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import type { Invoice, CreateInvoiceRequest } from "../types";

// Zod 스키마
const invoiceSchema = z.object({
  tenantId: z.string().min(1, "테넌트를 선택하세요"),
  amount: z.number().positive("금액은 0보다 커야 합니다"),
  issueDate: z.string().min(1, "발급일을 선택하세요"),
  dueDate: z.string().min(1, "마감일을 선택하세요"),
  description: z.string().optional(),
});

type FormData = z.infer<typeof invoiceSchema>;

interface InvoiceFormProps {
  initialData?: Invoice;
  onSubmit: (data: CreateInvoiceRequest) => void;
  onCancel: () => void;
  isSubmitting?: boolean;
}

export function InvoiceForm({
  initialData,
  onSubmit,
  onCancel,
  isSubmitting,
}: InvoiceFormProps) {
  const form = useForm<FormData>({
    resolver: zodResolver(invoiceSchema),
    defaultValues: initialData || {
      tenantId: "",
      amount: 0,
      issueDate: "",
      dueDate: "",
      description: "",
    },
  });

  return (
    <Form {...form}>
      <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-6">
        <FormField
          control={form.control}
          name="tenantId"
          render={({ field }) => (
            <FormItem>
              <FormLabel>테넌트 *</FormLabel>
              <FormControl>
                <Input
                  {...field}
                  placeholder="테넌트 선택"
                  disabled={isSubmitting}
                />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        <FormField
          control={form.control}
          name="amount"
          render={({ field }) => (
            <FormItem>
              <FormLabel>금액 *</FormLabel>
              <FormControl>
                <Input
                  type="number"
                  {...field}
                  onChange={(e) => field.onChange(parseFloat(e.target.value))}
                  disabled={isSubmitting}
                />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        <FormField
          control={form.control}
          name="issueDate"
          render={({ field }) => (
            <FormItem>
              <FormLabel>발급일 *</FormLabel>
              <FormControl>
                <Input type="date" {...field} disabled={isSubmitting} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        <FormField
          control={form.control}
          name="dueDate"
          render={({ field }) => (
            <FormItem>
              <FormLabel>마감일 *</FormLabel>
              <FormControl>
                <Input type="date" {...field} disabled={isSubmitting} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        <FormField
          control={form.control}
          name="description"
          render={({ field }) => (
            <FormItem>
              <FormLabel>설명</FormLabel>
              <FormControl>
                <Input
                  {...field}
                  placeholder="추가 설명 (선택사항)"
                  disabled={isSubmitting}
                />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        <div className="flex justify-end gap-2">
          <Button
            type="button"
            variant="outline"
            onClick={onCancel}
            disabled={isSubmitting}
          >
            취소
          </Button>
          <Button type="submit" disabled={isSubmitting}>
            {isSubmitting ? "처리 중..." : "저장"}
          </Button>
        </div>
      </form>
    </Form>
  );
}
```

### 5. Header 컴포넌트 (페이지 헤더)

**역할:**

- 페이지 제목/설명
- 주요 액션 버튼 (생성, 새로고침 등)

**구현 예시:**

```typescript
// features/invoice/components/invoice-header.tsx
"use client";

import { Plus, RefreshCw, Download } from "lucide-react";
import { Button } from "@/components/ui/button";
import { useInvoiceStore } from "../stores";

interface InvoiceHeaderProps {
  onRefresh?: () => void;
  onExport?: () => void;
}

export function InvoiceHeader({ onRefresh, onExport }: InvoiceHeaderProps) {
  const { openForm } = useInvoiceStore();

  return (
    <div className="flex items-center justify-between">
      <div>
        <h1 className="text-3xl font-bold tracking-tight">청구서 관리</h1>
        <p className="text-gray-600 mt-1">청구서를 생성하고 관리합니다</p>
      </div>
      <div className="flex gap-2">
        <Button variant="outline" size="sm" onClick={onRefresh}>
          <RefreshCw className="h-4 w-4 mr-2" />
          새로고침
        </Button>
        <Button variant="outline" size="sm" onClick={onExport}>
          <Download className="h-4 w-4 mr-2" />
          내보내기
        </Button>
        <Button size="sm" onClick={() => openForm()}>
          <Plus className="h-4 w-4 mr-2" />
          청구서 생성
        </Button>
      </div>
    </div>
  );
}
```

### 6. Filters 컴포넌트 (검색/필터)

**역할:**

- 검색 입력
- 필터 옵션
- Zustand 스토어 연동

**구현 예시:**

```typescript
// features/invoice/components/invoice-filters.tsx
"use client";

import { useMemo } from "react";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";
import { useInvoiceStore } from "../stores";
import type { Invoice } from "../types";

interface InvoiceFiltersProps {
  data: Invoice[];
}

export function InvoiceFilters({ data }: InvoiceFiltersProps) {
  const {
    globalFilter,
    setGlobalFilter,
    selectedStatus,
    setSelectedStatus,
    selectedTenant,
    setSelectedTenant,
    resetFilters,
  } = useInvoiceStore();

  const uniqueTenants = useMemo(() => {
    return Array.from(new Set(data.map((inv) => inv.tenantName)));
  }, [data]);

  return (
    <div className="space-y-4 p-4 border rounded-lg bg-gray-50">
      <div className="flex gap-4">
        {/* 검색 */}
        <Input
          placeholder="청구서 번호, 테넌트명 검색..."
          value={globalFilter}
          onChange={(e) => setGlobalFilter(e.target.value)}
          className="flex-1"
        />

        {/* 상태 필터 */}
        <Select value={selectedStatus} onValueChange={setSelectedStatus}>
          <SelectTrigger className="w-40">
            <SelectValue placeholder="상태 선택" />
          </SelectTrigger>
          <SelectContent>
            <SelectItem value="">전체</SelectItem>
            <SelectItem value="PAID">지급 완료</SelectItem>
            <SelectItem value="PENDING">미지급</SelectItem>
            <SelectItem value="OVERDUE">연체</SelectItem>
          </SelectContent>
        </Select>

        {/* 테넌트 필터 */}
        <Select value={selectedTenant} onValueChange={setSelectedTenant}>
          <SelectTrigger className="w-40">
            <SelectValue placeholder="테넌트 선택" />
          </SelectTrigger>
          <SelectContent>
            <SelectItem value="">전체</SelectItem>
            {uniqueTenants.map((tenant) => (
              <SelectItem key={tenant} value={tenant}>
                {tenant}
              </SelectItem>
            ))}
          </SelectContent>
        </Select>

        {/* 초기화 버튼 */}
        <Button variant="outline" onClick={resetFilters}>
          초기화
        </Button>
      </div>
    </div>
  );
}
```

### 7. Stats 컴포넌트 (통계 카드)

**역할:**

- 주요 지표 시각화
- 트렌드 정보 표시

**구현 예시:**

```typescript
// features/invoice/components/invoice-stats.tsx
"use client";

import { useMemo } from "react";
import { Building2, DollarSign, AlertCircle, CheckCircle } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import type { Invoice } from "../types";

interface InvoiceStatsProps {
  data: Invoice[];
}

export function InvoiceStats({ data }: InvoiceStatsProps) {
  const stats = useMemo(() => {
    const total = data.length;
    const paid = data.filter((inv) => inv.status === "PAID").length;
    const pending = data.filter((inv) => inv.status === "PENDING").length;
    const overdue = data.filter((inv) => inv.status === "OVERDUE").length;
    const totalAmount = data.reduce((sum, inv) => sum + inv.amount, 0);

    return [
      {
        title: "전체 청구서",
        value: total,
        icon: Building2,
        color: "primary",
      },
      {
        title: "총 금액",
        value: `₩${totalAmount.toLocaleString()}`,
        icon: DollarSign,
        color: "success",
      },
      {
        title: "지급 완료",
        value: paid,
        description: `${((paid / total) * 100).toFixed(1)}%`,
        icon: CheckCircle,
        color: "success",
      },
      {
        title: "연체",
        value: overdue,
        description: `${((overdue / total) * 100).toFixed(1)}%`,
        icon: AlertCircle,
        color: "warning",
      },
    ];
  }, [data]);

  return (
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
      {stats.map((stat, index) => (
        <Card key={index}>
          <CardContent className="pt-6">
            <div className="flex items-start justify-between">
              <div>
                <p className="text-sm font-medium text-gray-600">
                  {stat.title}
                </p>
                <p className="text-2xl font-bold mt-2">{stat.value}</p>
                {stat.description && (
                  <p className="text-xs text-gray-500 mt-1">
                    {stat.description}
                  </p>
                )}
              </div>
              <stat.icon className="h-8 w-8 text-gray-400" />
            </div>
          </CardContent>
        </Card>
      ))}
    </div>
  );
}
```

---

## 상태 관리

### 1. 로컬 상태 (useState)

단일 컴포넌트 내에서만 사용:

```typescript
const [isOpen, setIsOpen] = useState(false);
const [selectedId, setSelectedId] = useState<string | null>(null);
```

### 2. 서버 상태 (TanStack Query)

API에서 가져온 데이터:

```typescript
// hooks/use-invoices.ts
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { invoiceService } from "../services";

const invoicesKeys = {
  all: ["invoices"] as const,
  lists: () => [...invoicesKeys.all, "list"] as const,
  list: (params?: InvoiceQueryParams) =>
    [...invoicesKeys.lists(), params] as const,
  detail: (id: string) => [...invoicesKeys.all, "detail", id] as const,
};

export function useInvoices(params?: InvoiceQueryParams) {
  return useQuery({
    queryKey: invoicesKeys.list(params),
    queryFn: () => invoiceService.listInvoices(params),
    staleTime: 5 * 60 * 1000, // 5분
    retry: 2,
  });
}

export function useCreateInvoice(options?: {
  onSuccess?: (invoice: Invoice) => void;
  onError?: (error: Error) => void;
}) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateInvoiceRequest) =>
      invoiceService.createInvoice(data),
    onSuccess: (newInvoice) => {
      queryClient.invalidateQueries({
        queryKey: invoicesKeys.lists(),
      });
      options?.onSuccess?.(newInvoice);
    },
    onError: options?.onError,
  });
}
```

### 3. 전역 UI 상태 (Zustand)

UI 상태 관리 (모달, 필터, 페이징):

```typescript
// stores/invoice.store.ts
import { create } from "zustand";

interface InvoiceStoreState {
  // 모달 상태
  formOpen: boolean;
  editingId: string | null;

  // 필터 상태
  globalFilter: string;
  selectedStatus: string;
  selectedTenant: string;

  // 정렬 상태
  sorting: Array<{ id: string; desc: boolean }>;

  // 페이지네이션
  currentPage: number;
  itemsPerPage: number;

  // 액션
  openForm: (editingId?: string | null) => void;
  closeForm: () => void;
  setGlobalFilter: (filter: string) => void;
  setSelectedStatus: (status: string) => void;
  setSelectedTenant: (tenant: string) => void;
  resetFilters: () => void;
  setSorting: (sorting: Array<{ id: string; desc: boolean }>) => void;
  setCurrentPage: (page: number) => void;
  setItemsPerPage: (size: number) => void;
}

export const useInvoiceStore = create<InvoiceStoreState>((set) => ({
  formOpen: false,
  editingId: null,
  globalFilter: "",
  selectedStatus: "",
  selectedTenant: "",
  sorting: [],
  currentPage: 0,
  itemsPerPage: 20,

  openForm: (editingId = null) => set({ formOpen: true, editingId }),
  closeForm: () => set({ formOpen: false, editingId: null }),
  setGlobalFilter: (filter) => set({ globalFilter: filter }),
  setSelectedStatus: (status) => set({ selectedStatus: status }),
  setSelectedTenant: (tenant) => set({ selectedTenant: tenant }),
  resetFilters: () =>
    set({
      globalFilter: "",
      selectedStatus: "",
      selectedTenant: "",
      sorting: [],
      currentPage: 0,
    }),
  setSorting: (sorting) => set({ sorting }),
  setCurrentPage: (page) => set({ currentPage: page }),
  setItemsPerPage: (size) => set({ itemsPerPage: size, currentPage: 0 }),
}));
```

---

## 타입 정의 상세 가이드 (DTO 패턴)

### 개요

타입 파일은 **Entity, DTO, Query Parameters**를 구분하여 정의합니다. 이는 데이터베이스 스키마와 API 요청/응답 간의 차이를 명확히 합니다.

### 구조 및 패턴

**파일**: `features/[domain]/[entity]/types/[entity].types.ts`

```typescript
// features/tnnt/tenants/types/tenants.types.ts

// ========== ENTITY ==========
// 데이터베이스에서 반환되는 완전한 엔티티
export interface Tenant {
  // 시스템 필드
  id: string;
  created_at: string;
  created_by?: string;
  updated_at?: string;
  updated_by?: string;

  // 비즈니스 필드
  code: string;                    // 테넌트 코드 (예: TENANT-001)
  name: string;                    # 테넌트명
  type: TenantType;                # TRIAL, STANDARD, PREMIUM, ENTERPRISE
  status: TenantStatus;            # ACTIVE, SUSPENDED, TERMINATED
  start_date: string;              # YYYY-MM-DD
  end_date?: string;               # YYYY-MM-DD (optional)

  // 추가 정보
  biz_no?: string;                 # 사업자등록번호
  ceo_name?: string;               # 대표자명
  contact_email?: string;
  contact_phone?: string;
  address?: string;

  // 메타데이터
  is_deleted: boolean;
  extra_data?: Record<string, unknown>;
}

// ========== STATUS & TYPE ENUMS ==========
export type TenantStatus = "TRIAL" | "ACTIVE" | "SUSPENDED" | "TERMINATED";
export type TenantType = "TRIAL" | "STANDARD" | "PREMIUM" | "ENTERPRISE";

// ========== REQUEST DTO ==========
// 생성 요청 (필수 필드만)
export interface CreateTenantRequest {
  code: string;
  name: string;
  type: TenantType;
  start_date: string;

  // 선택사항
  biz_no?: string;
  ceo_name?: string;
  contact_email?: string;
  contact_phone?: string;
  address?: string;
}

// 수정 요청 (모든 필드 선택사항)
export interface UpdateTenantRequest {
  code?: string;
  name?: string;
  type?: TenantType;
  status?: TenantStatus;
  end_date?: string;
  biz_no?: string;
  ceo_name?: string;
  contact_email?: string;
  contact_phone?: string;
  address?: string;
}

// ========== RESPONSE DTO ==========
// 목록 응답
export interface TenantListResponse {
  data: Tenant[];
  total: number;
  page: number;
  pageSize: number;
  hasNextPage: boolean;
}

// 상세 조회 응답
export interface TenantDetailResponse {
  data: Tenant;
}

// ========== QUERY PARAMETERS ==========
export interface TenantQueryParams {
  // 페이지네이션
  page?: number;          // 1부터 시작
  pageSize?: number;      // 기본값: 20

  // 필터링
  search?: string;        # 코드, 이름 검색
  status?: TenantStatus;
  type?: TenantType;
  is_deleted?: boolean;

  // 정렬
  sortBy?: string;        # 정렬 필드: "name", "created_at", "status"
  sortOrder?: "asc" | "desc";
}
```

### Public API Index 작성

**파일**: `features/tnnt/tenants/types/index.ts`

```typescript
// features/tnnt/tenants/types/index.ts
export type {
  Tenant,
  TenantStatus,
  TenantType,
  CreateTenantRequest,
  UpdateTenantRequest,
  TenantListResponse,
  TenantDetailResponse,
  TenantQueryParams,
} from "./tenants.types";
```

---

## 서비스 레이어 (API 클라이언트)

### 에러 처리 및 Parameter Mapping

**파일**: `features/[domain]/[entity]/services/[entity].service.ts`

```typescript
// features/tnnt/tenants/services/tenants.service.ts
import { api } from "@/lib/api";
import type {
  Tenant,
  CreateTenantRequest,
  UpdateTenantRequest,
  TenantListResponse,
  TenantQueryParams,
} from "../types";

const ENDPOINT = "/api/v1/manager/tnnt/tenants";

export const tenantService = {
  // ========== 목록 조회 ==========
  async listTenants(
    params?: TenantQueryParams,
    signal?: AbortSignal
  ): Promise<TenantListResponse> {
    try {
      const response = await api.get<{ data: TenantListResponse }>(ENDPOINT, {
        params: {
          // camelCase → snake_case 변환
          page: params?.page,
          page_size: params?.pageSize, // ⭐ Parameter Mapping
          search: params?.search,
          status: params?.status,
          type: params?.type,
          is_deleted: params?.is_deleted,
          sort_by: params?.sortBy, // ⭐ Parameter Mapping
          sort_order: params?.sortOrder, // ⭐ Parameter Mapping
        },
        signal, // ⭐ AbortSignal for request cancellation
      });

      return (
        response.data.data || {
          data: [],
          total: 0,
          page: 1,
          pageSize: 20,
          hasNextPage: false,
        }
      );
    } catch (error) {
      // ⭐ 에러 처리
      throw ApiError.fromAxiosError(error, "listTenants");
    }
  },

  // ========== 단일 조회 ==========
  async getTenant(id: string, signal?: AbortSignal): Promise<Tenant> {
    try {
      const response = await api.get<{ data: Tenant }>(`${ENDPOINT}/${id}`, {
        signal,
      });
      return response.data.data;
    } catch (error) {
      throw ApiError.fromAxiosError(error, `getTenant(${id})`);
    }
  },

  // ========== 생성 ==========
  async createTenant(data: CreateTenantRequest): Promise<Tenant> {
    try {
      const response = await api.post<{ data: Tenant }>(ENDPOINT, data);
      return response.data.data;
    } catch (error) {
      throw ApiError.fromAxiosError(error, "createTenant");
    }
  },

  // ========== 수정 ==========
  async updateTenant(id: string, data: UpdateTenantRequest): Promise<Tenant> {
    try {
      const response = await api.put<{ data: Tenant }>(
        `${ENDPOINT}/${id}`,
        data
      );
      return response.data.data;
    } catch (error) {
      throw ApiError.fromAxiosError(error, `updateTenant(${id})`);
    }
  },

  // ========== 삭제 ==========
  async deleteTenant(id: string): Promise<void> {
    try {
      await api.delete(`${ENDPOINT}/${id}`);
    } catch (error) {
      throw ApiError.fromAxiosError(error, `deleteTenant(${id})`);
    }
  },
};
```

### 에러 클래스 (ApiError)

**파일**: `lib/errors.ts`

```typescript
// lib/errors.ts
import type { AxiosError } from "axios";

export class ApiError extends Error {
  public readonly statusCode: number;
  public readonly data?: unknown;
  public readonly operation: string;

  constructor(
    message: string,
    statusCode: number = 500,
    operation: string = "unknown",
    data?: unknown
  ) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.operation = operation;
    this.data = data;
  }

  // ⭐ Axios 에러를 ApiError로 변환
  static fromAxiosError(
    error: unknown,
    operation: string = "unknown"
  ): ApiError {
    if (error instanceof AxiosError) {
      const statusCode = error.response?.status || 500;
      const message =
        error.response?.data?.message ||
        error.message ||
        "알 수 없는 오류 발생";

      return new ApiError(message, statusCode, operation, error.response?.data);
    }

    return new ApiError(
      error instanceof Error ? error.message : "알 수 없는 오류",
      500,
      operation
    );
  }

  // ⭐ 사용자 친화적 메시지
  getUserMessage(): string {
    switch (this.statusCode) {
      case 400:
        return "잘못된 요청입니다. 입력 값을 확인해주세요.";
      case 401:
        return "인증이 필요합니다. 다시 로그인해주세요.";
      case 403:
        return "이 작업을 수행할 권한이 없습니다.";
      case 404:
        return "요청한 데이터를 찾을 수 없습니다.";
      case 409:
        return "데이터 충돌이 발생했습니다. 다시 시도해주세요.";
      case 500:
        return "서버 오류가 발생했습니다. 관리자에게 문의해주세요.";
      default:
        return this.message;
    }
  }
}
```

### Public API Index

**파일**: `features/tnnt/tenants/services/index.ts`

```typescript
export { tenantService } from "./tenants.service";
```

---

## TanStack Query Hook (Query Key Factory 패턴)

### Query Key Factory 정의

**파일**: `features/[domain]/[entity]/hooks/use-[entity].ts`

```typescript
// features/tnnt/tenants/hooks/use-tenants.ts
import {
  useQuery,
  useMutation,
  useQueryClient,
  type UseQueryOptions,
  type UseMutationOptions,
} from "@tanstack/react-query";
import { tenantService } from "../services";
import type {
  Tenant,
  TenantQueryParams,
  CreateTenantRequest,
  UpdateTenantRequest,
} from "../types";

// ⭐ Query Key Factory (계층적 캐시 관리)
export const tenantsKeys = {
  // 모든 테넌트 관련 쿼리의 루트 키
  all: ["tenants"] as const,

  // 목록 관련 모든 쿼리
  lists: () => [...tenantsKeys.all, "list"] as const,

  // 특정 파라미터로 필터링된 목록
  list: (params?: TenantQueryParams) =>
    [...tenantsKeys.lists(), { ...params }] as const,

  // 상세 조회 관련
  details: () => [...tenantsKeys.all, "detail"] as const,

  // 특정 ID의 상세 조회
  detail: (id: string) => [...tenantsKeys.details(), id] as const,
};

// ========== useQuery Hooks ==========

export function useTenants(
  params?: TenantQueryParams,
  options?: UseQueryOptions<any>
) {
  return useQuery({
    queryKey: tenantsKeys.list(params),
    queryFn: ({ signal }) => tenantService.listTenants(params, signal),
    staleTime: 5 * 60 * 1000, // 5분
    gcTime: 10 * 60 * 1000, // 10분 (구 cacheTime)
    retry: 2,
    ...options,
  });
}

export function useTenant(id: string) {
  return useQuery({
    queryKey: tenantsKeys.detail(id),
    queryFn: ({ signal }) => tenantService.getTenant(id, signal),
    staleTime: 5 * 60 * 1000,
    gcTime: 10 * 60 * 1000,
    enabled: !!id, // ID가 있을 때만 쿼리 실행
  });
}

// ========== useMutation Hooks ==========

export function useCreateTenant(
  options?: UseMutationOptions<Tenant, Error, CreateTenantRequest>
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data) => tenantService.createTenant(data),
    onSuccess: (newTenant) => {
      // 목록 쿼리 무효화 (새 항목이 추가되었으므로)
      queryClient.invalidateQueries({
        queryKey: tenantsKeys.lists(),
      });
      options?.onSuccess?.(newTenant);
    },
    onError: (error) => {
      options?.onError?.(error);
    },
  });
}

export function useUpdateTenant(
  id: string,
  options?: UseMutationOptions<Tenant, Error, UpdateTenantRequest>
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data) => tenantService.updateTenant(id, data),
    onSuccess: (updatedTenant) => {
      // 목록과 상세 모두 무효화
      queryClient.invalidateQueries({
        queryKey: tenantsKeys.lists(),
      });
      queryClient.invalidateQueries({
        queryKey: tenantsKeys.detail(id),
      });
      options?.onSuccess?.(updatedTenant);
    },
  });
}

export function useDeleteTenant(
  options?: UseMutationOptions<void, Error, string>
) {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id) => tenantService.deleteTenant(id),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: tenantsKeys.lists(),
      });
      options?.onSuccess?.();
    },
  });
}
```

### Public API Index

**파일**: `features/tnnt/tenants/hooks/index.ts`

```typescript
export {
  tenantsKeys,
  useTenants,
  useTenant,
  useCreateTenant,
  useUpdateTenant,
  useDeleteTenant,
} from "./use-tenants";
```

---

## Zustand Store (UI 상태 분리)

### 상태 분리 패턴

**파일**: `features/[domain]/[entity]/stores/[entity].store.ts`

```typescript
// features/tnnt/tenants/stores/tenants.store.ts
import { create } from "zustand";
import type { Updater } from "zustand";
import type { TenantStatus, TenantType } from "../types";

interface TenantsStoreState {
  // ========== 모달/폼 상태 ==========
  formOpen: boolean;
  editingId: string | null;

  openForm: (editingId?: string | null) => void;
  closeForm: () => void;

  // ========== 필터 상태 ==========
  globalFilter: string; // 검색어
  selectedStatus: TenantStatus | "";
  selectedType: TenantType | "";

  setGlobalFilter: (filter: Updater<string>) => void;
  setSelectedStatus: (status: TenantStatus | "") => void;
  setSelectedType: (type: TenantType | "") => void;

  // ========== 정렬 상태 ==========
  sorting: Array<{ id: string; desc: boolean }>;
  setSorting: (sorting: Updater<Array<{ id: string; desc: boolean }>>) => void;

  // ========== 페이지네이션 상태 ==========
  currentPage: number;
  itemsPerPage: number;

  setCurrentPage: (page: number) => void;
  setItemsPerPage: (size: number) => void;

  // ========== 초기화 ==========
  resetFilters: () => void;
  reset: () => void;
}

const initialState = {
  formOpen: false,
  editingId: null,
  globalFilter: "",
  selectedStatus: "" as TenantStatus | "",
  selectedType: "" as TenantType | "",
  sorting: [],
  currentPage: 0,
  itemsPerPage: 20,
};

export const useTenantsStore = create<TenantsStoreState>((set) => ({
  ...initialState,

  // ========== 모달/폼 액션 ==========
  openForm: (editingId = null) => set({ formOpen: true, editingId }),

  closeForm: () => set({ formOpen: false, editingId: null }),

  // ========== 필터 액션 ==========
  setGlobalFilter: (filter) =>
    set((state) => ({
      globalFilter:
        typeof filter === "function" ? filter(state.globalFilter) : filter,
      currentPage: 0, // 필터 변경 시 첫 페이지로
    })),

  setSelectedStatus: (status) =>
    set({ selectedStatus: status, currentPage: 0 }),

  setSelectedType: (type) => set({ selectedType: type, currentPage: 0 }),

  // ========== 정렬 액션 ==========
  setSorting: (sorting) =>
    set((state) => ({
      sorting: typeof sorting === "function" ? sorting(state.sorting) : sorting,
      currentPage: 0, // 정렬 변경 시 첫 페이지로
    })),

  // ========== 페이지네이션 액션 ==========
  setCurrentPage: (page) => set({ currentPage: page }),

  setItemsPerPage: (size) => set({ itemsPerPage: size, currentPage: 0 }),

  // ========== 초기화 액션 ==========
  resetFilters: () =>
    set({
      globalFilter: "",
      selectedStatus: "",
      selectedType: "",
      sorting: [],
      currentPage: 0,
    }),

  reset: () => set(initialState),
}));
```

### Public API Index

**파일**: `features/tnnt/tenants/stores/index.ts`

```typescript
export { useTenantsStore } from "./tenants.store";
```

---

## 에러 처리 전략

### 컴포넌트 레벨 에러 처리

```typescript
// features/tnnt/tenants/components/tenants-table.tsx
"use client";

import { toast } from "sonner";
import { useTenants } from "../hooks";
import { DataTable } from "@/components/ui/data-table";
import { getTenantsColumns } from "./tenants-columns";
import type { TenantQueryParams } from "../types";

export function TenantsTable(params?: TenantQueryParams) {
  const { data, isLoading, error } = useTenants(params);

  // ⭐ 에러 처리
  if (error) {
    toast.error(error.message || "데이터를 불러오는데 실패했습니다");
    return (
      <div className="p-4 text-center text-red-600">
        데이터를 불러오는데 실패했습니다. 다시 시도해주세요.
      </div>
    );
  }

  const columns = getTenantsColumns();

  return (
    <DataTable
      columns={columns}
      data={data?.data || []}
      loading={isLoading}
      emptyMessage="등록된 테넌트가 없습니다"
    />
  );
}
```

### Mutation 에러 처리

```typescript
// features/tnnt/tenants/components/tenants-form.tsx
"use client";

import { toast } from "sonner";
import { useCreateTenant, useUpdateTenant } from "../hooks";
import { ApiError } from "@/lib/errors";

export function TenantsForm() {
  const { mutate: createTenant } = useCreateTenant({
    onSuccess: () => {
      toast.success("테넌트가 생성되었습니다");
    },
    onError: (error) => {
      // ⭐ ApiError 인스턴스 확인
      if (error instanceof ApiError) {
        toast.error(error.getUserMessage());
      } else {
        toast.error("생성 실패");
      }
    },
  });

  return (
    <form
      onSubmit={(e) => {
        e.preventDefault();
        createTenant({
          name: "New Tenant",
          code: "TNT-001",
          type: "STANDARD",
          start_date: "2024-01-01",
        });
      }}
    >
      {/* 폼 필드 */}
    </form>
  );
}
```

---

## Feature Index (공개 API 패턴)

### 최상위 Feature Index

**파일**: `features/tnnt/tenants/index.ts`

```typescript
// features/tnnt/tenants/index.ts
// ⭐ Public API: 외부에서 가져갈 수 있는 것만 export

// Components (UI 구성 요소)
export {
  TenantsColumns,
  TenantsTable,
  TenantsEdit,
  TenantsForm,
  TenantsHeader,
  TenantsFilters,
  TenantsStats,
} from "./components";

// Hooks (비즈니스 로직)
export {
  tenantsKeys,
  useTenants,
  useTenant,
  useCreateTenant,
  useUpdateTenant,
  useDeleteTenant,
} from "./hooks";

// Services (API 통신 - 필요한 경우만)
export { tenantService } from "./services";

// Store (상태 관리)
export { useTenantsStore } from "./stores";

// Types (타입 정의)
export type {
  Tenant,
  TenantStatus,
  TenantType,
  CreateTenantRequest,
  UpdateTenantRequest,
  TenantListResponse,
  TenantQueryParams,
} from "./types";
```

### Domain-level Index

**파일**: `features/tnnt/index.ts`

```typescript
// features/tnnt/index.ts
// 같은 도메인의 모든 엔티티 export

export * from "./tenants";
export * from "./subscriptions";
export * from "./tenant-roles";
export * from "./tenant-users";
```

---

## API 통합

### API 클라이언트 설정

```typescript
// lib/api.ts
import axios from "axios";

export const api = axios.create({
  baseURL: process.env.NEXT_PUBLIC_API_URL,
  headers: { "Content-Type": "application/json" },
  timeout: 10000,
});

// 요청 인터셉터 (JWT 토큰 추가)
api.interceptors.request.use((config) => {
  const token = localStorage.getItem("access_token");
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 응답 인터셉터 (에러 처리)
api.interceptors.response.use(
  (response) => response,
  async (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem("access_token");
      window.location.href = "/login";
    }
    return Promise.reject(error);
  }
);
```

### API 서비스 작성

```typescript
// features/invoice/services/invoice.service.ts
import { api } from "@/lib/api";
import type {
  Invoice,
  InvoiceListResponse,
  CreateInvoiceRequest,
  UpdateInvoiceRequest,
  InvoiceQueryParams,
} from "../types";

const ENDPOINT = "/api/v1/manager/bill/invoices";

export const invoiceService = {
  async listInvoices(
    params?: InvoiceQueryParams
  ): Promise<InvoiceListResponse> {
    const response = await api.get<{ data: InvoiceListResponse }>(ENDPOINT, {
      params: {
        page: params?.page,
        page_size: params?.pageSize,
        search: params?.search,
        status: params?.status,
      },
    });
    return response.data.data;
  },

  async getInvoice(id: string): Promise<Invoice> {
    const response = await api.get<{ data: Invoice }>(`${ENDPOINT}/${id}`);
    return response.data.data;
  },

  async createInvoice(data: CreateInvoiceRequest): Promise<Invoice> {
    const response = await api.post<{ data: Invoice }>(ENDPOINT, data);
    return response.data.data;
  },

  async updateInvoice(
    id: string,
    data: UpdateInvoiceRequest
  ): Promise<Invoice> {
    const response = await api.put<{ data: Invoice }>(
      `${ENDPOINT}/${id}`,
      data
    );
    return response.data.data;
  },

  async deleteInvoice(id: string): Promise<void> {
    await api.delete(`${ENDPOINT}/${id}`);
  },
};
```

### 타입 정의

```typescript
// features/invoice/types/invoice.types.ts

// 엔티티 타입
export interface Invoice {
  id: string;
  invoiceNumber: string;
  tenantId: string;
  tenantName: string;
  amount: number;
  status: InvoiceStatus;
  issueDate: string;
  dueDate: string;
  description?: string;
  created_at: string;
  updated_at?: string;
}

// 상태 enum
export type InvoiceStatus = "PAID" | "PENDING" | "OVERDUE" | "CANCELLED";

// 요청 DTO
export interface CreateInvoiceRequest {
  tenantId: string;
  amount: number;
  issueDate: string;
  dueDate: string;
  description?: string;
}

export interface UpdateInvoiceRequest {
  amount?: number;
  status?: InvoiceStatus;
  dueDate?: string;
  description?: string;
}

// 목록 응답
export interface InvoiceListResponse {
  items: Invoice[];
  total: number;
  page: number;
  page_size: number;
  total_pages: number;
}

// 쿼리 파라미터
export interface InvoiceQueryParams {
  page?: number;
  pageSize?: number;
  search?: string;
  status?: InvoiceStatus;
  tenant?: string;
}
```

---

## 목록 페이지 구성

### 완전한 페이지 통합

```typescript
// app/(app)/manager/bill/invoices/page.tsx
"use client";

import { toast } from "sonner";
import {
  InvoiceHeader,
  InvoiceStats,
  InvoiceFilters,
  InvoiceTable,
  InvoiceEdit,
} from "@/features/invoice";
import { useInvoices, useDeleteInvoice } from "@/features/invoice/hooks";
import { useInvoiceStore } from "@/features/invoice/stores";

export default function InvoicesPage() {
  const {
    globalFilter,
    selectedStatus,
    selectedTenant,
    currentPage,
    itemsPerPage,
  } = useInvoiceStore();

  // 조회
  const {
    data: invoicesResponse,
    isLoading,
    error,
    refetch,
  } = useInvoices({
    page: currentPage + 1,
    pageSize: itemsPerPage,
    search: globalFilter,
    status: selectedStatus as any,
    tenant: selectedTenant,
  });

  // 삭제
  const { mutate: deleteInvoice } = useDeleteInvoice({
    onSuccess: () => toast.success("청구서가 삭제되었습니다"),
    onError: (error) => toast.error(error.message || "삭제 실패"),
  });

  const invoices = invoicesResponse?.items || [];
  const totalItems = invoicesResponse?.total || 0;

  if (error) {
    return <div className="p-4 text-red-600">에러: {error.message}</div>;
  }

  return (
    <div className="space-y-6 p-6">
      {/* 헤더 */}
      <InvoiceHeader onRefresh={() => refetch()} />

      {/* 통계 */}
      <InvoiceStats data={invoices} />

      {/* 필터 */}
      <InvoiceFilters data={invoices} />

      {/* 테이블 */}
      <InvoiceTable
        data={invoices}
        total={totalItems}
        isLoading={isLoading}
        onEdit={(invoice) => {
          // Edit 모달 열기
        }}
        onDelete={(invoice) => {
          deleteInvoice(invoice.id);
        }}
      />

      {/* Edit 모달 */}
      <InvoiceEdit />
    </div>
  );
}
```

---

## 스타일링 시스템

### Tailwind CSS v4 구조

```css
/* globals.css */
@import "tailwindcss";
@import "tw-animate-css";

@custom-variant dark (&:is(.dark *));

@theme {
  --font-sans: var(--font-geist-sans);
  --color-primary: var(--primary);
}

:root {
  /* 라이트 모드 색상 */
  --primary: oklch(54% 0.25 293);
  --background: oklch(98% 0 0);
  --foreground: oklch(20% 0 0);
}

.dark {
  /* 다크 모드 색상 */
  --primary: oklch(60% 0.25 293);
  --background: oklch(20% 0 0);
  --foreground: oklch(98% 0 0);
}
```

### 클래스명 순서 규칙

```typescript
<div className="
  flex items-center justify-between     // Layout
  p-4 mt-2 mb-4                         // Spacing
  w-full h-auto                         // Sizing
  text-lg font-bold                     // Typography
  bg-white border rounded-lg shadow-sm  // Visual
  hover:bg-gray-50 cursor-pointer       // Interactive
  md:flex-row md:p-6                    // Responsive
  dark:bg-gray-800                      // Dark mode
">
```

### 색상 시스템 사용

```typescript
// ✅ 권장: 시맨틱 변수 사용
<div className="bg-primary text-primary-foreground">
  Primary 색상
</div>

// ❌ 피해야 할 것: 하드코딩
<div className="bg-violet-600 text-white">
  지양
</div>
```

---

## 베스트 프랙티스

### ✅ DO (권장)

**1. 타입 안전성**

```typescript
// ✅ 좋음
interface UserProps {
  id: number;
  name: string;
  email?: string;
}

export function User({ id, name, email }: UserProps) {
  return <div>{name}</div>;
}
```

**2. Early Return 패턴**

```typescript
// ✅ 좋음
function UserList() {
  const { users, isLoading, error } = useUsers();

  if (isLoading) return <LoadingSpinner />;
  if (error) return <ErrorMessage />;
  if (!users?.length) return <EmptyState />;

  return <UserTable users={users} />;
}
```

**3. 컴포넌트 구성 (Composition)**

```typescript
// ✅ 좋음: 작은 컴포넌트 조합
<div>
  <Header />
  <Stats />
  <Filters />
  <Table />
</div>
```

### ❌ DON'T (피해야 할 것)

**1. any 타입 사용**

```typescript
// ❌ 나쁨
function handleClick(e: any) {}

// ✅ 좋음
function handleClick(e: React.MouseEvent<HTMLButtonElement>) {}
```

**2. Props Drilling (3단계 이상)**

```typescript
// ❌ 나쁨
<Parent data={data} onUpdate={onUpdate} />
  <Child data={data} onUpdate={onUpdate} />
    <Grandchild data={data} onUpdate={onUpdate} />

// ✅ 좋음: Context 또는 Zustand 사용
```

**3. 인라인 스타일**

```typescript
// ❌ 나쁨
<div style={{ backgroundColor: '#8b5cf6' }}>

// ✅ 좋음
<div className="bg-violet-600">
```

---

## 체크리스트

### 새로운 Feature 추가 시

- [ ] Feature 디렉토리 구조 생성
- [ ] 타입 정의 작성 (Entity, DTO, QueryParams)
- [ ] API 서비스 작성 (CRUD)
- [ ] TanStack Query hooks 작성
- [ ] Zustand store 작성 (UI 상태)
- [ ] 로딩/에러 상태 처리
- [ ] 접근성 고려 (ARIA, 키보드)
- [ ] 반응형 디자인
- [ ] 성능 최적화 검토

### 목록 페이지 추가 시 (필수!)

- [ ] **7개 필수 컴포넌트 작성**
  - [ ] `[domain]-columns.tsx` - 테이블 컬럼 정의
  - [ ] `[domain]-table.tsx` - 테이블 렌더링
  - [ ] `[domain]-edit.tsx` - 수정 모달
  - [ ] `[domain]-form.tsx` - 생성/수정 폼
  - [ ] `[domain]-header.tsx` - 페이지 헤더
  - [ ] `[domain]-filters.tsx` - 검색/필터
  - [ ] `[domain]-stats.tsx` - 통계 카드
- [ ] 메인 페이지에 통합
- [ ] Query Key Factory 정의
- [ ] 필터/검색/정렬 구현
- [ ] 서버 사이드 페이징 구현
- [ ] 빈 데이터 상태 처리
- [ ] 에러 상태 처리

---

## 참고 자료

- [Next.js 문서](https://nextjs.org/docs)
- [React 문서](https://react.dev)
- [TanStack Query](https://tanstack.com/query)
- [Zustand](https://github.com/pmndrs/zustand)
- [Tailwind CSS v4](https://tailwindcss.com)
- [shadcn/ui](https://ui.shadcn.com)

---

**개발을 즐기세요! 🚀**
