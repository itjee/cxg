# ConexGrow 스키마 개선 요약 - 한 페이지 체크리스트

## 🎯 핵심 발견사항

| 항목 | 상태 | 심각도 | 액션 |
|------|------|--------|------|
| **세금계산서 중복** | srm.sales_invoices vs fim.tax_invoices | 🔴 높음 | 통합 필요 |
| **휴가/결근 부족** | hrm.absences 없음 | 🔴 높음 | 즉시 추가 |
| **고객 피드백 부족** | asm.customer_feedback 없음 | 🟡 중간 | 1개월 내 추가 |
| **계약 관리 부족** | crm.contracts 없음 | 🟡 중간 | 3개월 내 추가 |
| **프로모션 관리 부족** | srm.promotions 없음 | 🟡 중간 | 3개월 내 추가 |
| **공통코드 분리** | adm vs com | ✅ 적절 | 개선 불필요 |

---

## 📋 P1 (즉시 실행) - 2주

### 1. 세금계산서 통합
**현황**: 2개 테이블 (SRM + FIM)
**문제**: 데이터 정규화 위반, 동기화 어려움

| 액션 | 상세 | 파일 |
|------|------|------|
| 통합 | fim.tax_invoices를 마스터로 지정 | 14_fim/08_tax_invoices.sql |
| 확장 | sales 관련 필드 추가 (so_id, delivery_id) | 수정 필요 |
| 제거 | srm.sales_invoices 제거 | 12_srm/07_sales_invoices.sql |
| VIEW | 하위호환성을 위한 뷰 생성 | 신규 생성 |

**SQL 변경사항:**
```sql
-- FIM에 추가
ALTER TABLE fim.tax_invoices ADD COLUMN (
  sales_order_id UUID,
  sales_delivery_id UUID
);

-- SRM에서 VIEW로 변경
CREATE VIEW srm.vw_sales_invoices AS
SELECT ... FROM fim.tax_invoices WHERE ...;

-- 또는 SRM에서 완전 제거
DROP TABLE srm.sales_invoices;
```

**영향도**: ⭐⭐⭐⭐⭐ (높음 - 판매 + 회계)

---

### 2. 휴가/결근 관리 추가
**현황**: hrm.attendances만 있음 (출근 기록만)
**필요성**: 급여 계산, 근로법 준수 필수

| 테이블 | 목적 | 주요 필드 |
|--------|------|----------|
| **hrm.absences** | 결근/휴가 기록 | employee_id, absence_type, date_from, date_to, status |
| **hrm.leave_policies** | 휴가 정책 | leave_type, entitled_days_per_year, is_paid |

**SQL:**
```sql
-- 파일: 02_hrm/08_absences.sql
CREATE TABLE hrm.absences (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  employee_id UUID NOT NULL REFERENCES hrm.employees(id),
  absence_type VARCHAR(20) NOT NULL
    CHECK (absence_type IN ('ABSENCE', 'LATE', 'SICK', 'ANNUAL', 'UNPAID', 'MATERNITY')),
  date_from DATE NOT NULL,
  date_to DATE NOT NULL,
  duration_hours INTEGER,
  reason TEXT,
  approved_by UUID REFERENCES hrm.employees(id),
  status VARCHAR(20) DEFAULT 'DRAFT'
    CHECK (status IN ('DRAFT', 'SUBMITTED', 'APPROVED', 'REJECTED')),
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
  created_by UUID,
  updated_at TIMESTAMP WITH TIME ZONE,
  updated_by UUID,
  is_deleted BOOLEAN DEFAULT false
);

-- 파일: 02_hrm/09_leave_policies.sql
CREATE TABLE hrm.leave_policies (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  leave_type VARCHAR(20) NOT NULL UNIQUE,
  entitled_days_per_year INTEGER NOT NULL,
  is_paid BOOLEAN NOT NULL,
  description TEXT,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
  created_by UUID,
  is_active BOOLEAN DEFAULT true
);
```

**영향도**: ⭐⭐⭐⭐ (높음 - 급여 연동)

---

### 3. 고객 피드백 추가
**현황**: asm.support_tickets만 있음 (지원만)
**필요성**: CRM 고도화, 만족도 추적

| 테이블 | 목적 | 주요 필드 |
|--------|------|----------|
| **asm.customer_feedback** | 고객 평가/피드백 | partner_id, rating (1-5), comment, status |
| **asm.nps_surveys** | NPS 설문 | partner_id, nps_score (0-10), recommendation |

**SQL (간단 버전):**
```sql
-- 파일: 13_asm/07_customer_feedback.sql
CREATE TABLE asm.customer_feedback (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  partner_id UUID NOT NULL REFERENCES crm.partners(id),
  transaction_type VARCHAR(20) CHECK (transaction_type IN ('SALE', 'SERVICE', 'SUPPORT')),
  transaction_id UUID,
  rating INTEGER NOT NULL CHECK (rating BETWEEN 1 AND 5),
  comment TEXT,
  response_text TEXT,
  response_by UUID REFERENCES hrm.employees(id),
  response_date TIMESTAMP WITH TIME ZONE,
  status VARCHAR(20) DEFAULT 'NEW'
    CHECK (status IN ('NEW', 'REVIEWED', 'RESPONDED', 'CLOSED')),
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
  created_by UUID,
  updated_at TIMESTAMP WITH TIME ZONE,
  updated_by UUID,
  is_deleted BOOLEAN DEFAULT false
);
```

**영향도**: ⭐⭐⭐ (중간 - CRM 기능 확대)

---

## 📋 P2 (1-3개월 내) - 단계적 추가

### 4. 거래처 계약 관리
**추가 위치**: 03_CRM 모듈
**파일**: `03_crm/19_contracts.sql`, `03_crm/20_contract_items.sql`

**주요 필드:**
```
crm.contracts:
  - partner_id, contract_code, contract_type
  - contract_date, start_date, end_date, renewal_date
  - contract_amount, auto_renewal
  - signatory_company, signatory_partner
  - status (ACTIVE, EXPIRED, TERMINATED, SUSPENDED)

crm.contract_items:
  - contract_id, item_seq, item_description, item_value
```

**이유**: 중소기업은 거래처와의 계약 관리 필수

---

### 5. 프로모션/할인 관리
**추가 위치**: 12_SRM 모듈
**파일**: `12_srm/12_promotions.sql`, `12_srm/13_promotion_usage.sql`

**주요 필드:**
```
srm.promotions:
  - promotion_code, promotion_name, promotion_type
  - discount_percent, discount_amount
  - min_order_amount, max_discount_amount
  - start_date, end_date
  - customer_segment_id, product_id
  - is_active

srm.promotion_usage:
  - promotion_id, sales_order_id, discount_applied, applied_at
```

**이유**: 계절 프로모션, 벌크 할인 관리 필수

---

### 6. 반품/환불 정책
**추가 위치**: 13_ASM 모듈
**파일**: `13_asm/09_return_policies.sql`, `13_asm/10_return_approvals.sql`

**주요 필드:**
```
asm.return_policies:
  - policy_code, product_category_id
  - return_period_days, restocking_fee_percent
  - condition_required, refund_method
  - is_active

asm.return_approvals:
  - sales_return_id, policy_id
  - refund_amount, approved_by, approval_date
  - status, approval_reason
```

**이유**: 반품 승인 워크플로우, 환불 규정 명확화

---

## 📊 모듈별 현황

### 완성도 순서

| 순위 | 모듈 | 상태 | 추가 필요 |
|------|------|------|----------|
| 1 | 04_PIM (상품관리) | ⭐⭐⭐⭐⭐ | 불필요 |
| 2 | 14_FIM (재무관리) | ⭐⭐⭐⭐⭐ | 통합 필요 |
| 3 | 10_IVM (재고관리) | ⭐⭐⭐⭐⭐ | 불필요 |
| 4 | 01_ADM (기본설정) | ⭐⭐⭐⭐⭐ | 결제조건 추가 |
| 5 | 11_PSM (구매관리) | ⭐⭐⭐⭐ | 불필요 |
| 6 | 15_FAM (자산관리) | ⭐⭐⭐⭐ | 불필요 |
| 7 | 05_WMS (창고관리) | ⭐⭐⭐⭐ | 불필요 |
| 8 | 03_CRM (거래처관리) | ⭐⭐⭐⭐ | 계약 추가 ⭐ |
| 9 | 02_HRM (인사관리) | ⭐⭐⭐⭐ | 휴가 추가 ⭐ |
| 10 | 06_APM (결재관리) | ⭐⭐⭐⭐ | 불필요 |
| 11 | 22_SYS (시스템) | ⭐⭐⭐⭐ | 불필요 |
| 12 | 21_COM (공통코드) | ⭐⭐⭐⭐ | 결제조건 추가 |
| 13 | 12_SRM (판매관리) | ⭐⭐⭐ | 프로모션 추가 ⭐ |
| 14 | 13_ASM (서비스관리) | ⭐⭐⭐ | 피드백 추가 ⭐ |
| 15 | 20_BIM (분석관리) | ⭐⭐⭐ | KPI 확대 |

---

## ⚙️ 기술 체크리스트

### 설계 검토
- [ ] P1 테이블 스키마 설계서 작성 및 검토
- [ ] 외래키 관계 검증
- [ ] 인덱스 전략 수립
- [ ] 데이터 마이그레이션 계획 수립

### 개발
- [ ] SQL 스크립트 작성
- [ ] 알렘비(Alembic) 마이그레이션 생성
- [ ] 백엔드 모델/리포지토리 추가
- [ ] API 엔드포인트 작성

### 테스트
- [ ] 단위 테스트 작성
- [ ] 통합 테스트 실행
- [ ] 데이터 정합성 검증
- [ ] 성능 테스트 (대용량 데이터)

### 배포
- [ ] 개발 환경 배포 및 검증
- [ ] 스테이징 환경 배포 및 검증
- [ ] 프로덕션 배포 계획
- [ ] 롤백 계획 수립

### 문서화
- [ ] 스키마 변경 문서
- [ ] API 문서 업데이트
- [ ] 운영 매뉴얼 작성
- [ ] 마이그레이션 가이드 작성

---

## 📅 일정 (추천)

```
Week 1-2: P1 실행 (세금계산서, 휴가, 피드백)
  - Mon: 설계 리뷰
  - Tue-Wed: SQL 작성 및 검증
  - Thu-Fri: 애플리케이션 코드 작업

Week 3-4: 테스트 및 배포
  - 통합 테스트
  - 프로덕션 배포

Month 2-3: P2 실행 (계약, 프로모션, 반품)
  - 단계별 추가
  - 기능 테스트
```

---

## 💡 추가 조언

### 즉시 할 일
1. ✅ 이 리포트를 팀과 공유
2. ✅ P1 항목에 대한 타임라인 확정
3. ✅ 각 개발팀에 할당

### 주의사항
- 세금계산서 통합은 **기존 시스템 영향이 크므로** 충분한 테스트 필수
- 휴가 데이터는 **급여 계산 연동** 검토 필수
- 모든 신규 테이블은 **권한 관리** 검토 필수

### 성공 지표
- 데이터 정합성 100%
- 테스트 커버리지 90%+
- 배포 후 이상 없음
- 운영팀 매뉴얼 이해도 100%

---

**담당**: 데이터베이스 아키텍트 / 개발 리더
**우선 검토 대상**: CTO, 재무/회계팀, HR팀
