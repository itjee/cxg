# CSM-ASM-CRM 통합 분석 및 권장안

**분석일**: 2025-01-20  
**대상**: csm (Customer Support Management) 스키마  
**목적**: csm을 asm 또는 crm과 통합할지 결정  

---

## 📋 현황 분석

### CSM (Customer Support Management) - 고객 지원

**위치**: `_deprecated/csm.sql.review` (140 lines)

**테이블 구조**:
```sql
csm.support_tickets      -- 고객 지원 티켓
  ├── ticket_code
  ├── customer_id        -- 고객 참조
  ├── subject, description
  ├── category, priority, status
  ├── assigned_to        -- 담당자
  ├── resolved_at, resolution
  └── satisfaction_rating

csm.ticket_comments      -- 티켓 댓글/이력
  ├── ticket_id
  ├── comment_text
  └── is_internal        -- 내부 메모 여부

csm.faqs                 -- 자주 묻는 질문
  ├── category
  ├── question, answer
  └── view_count
```

**용도**: 
- 고객 문의/불만 접수
- 기술 지원 티켓
- 제품 문의
- FAQ 관리

---

### ASM (After-Sales Management) - A/S 관리

**위치**: `13_asm.sql`

**테이블 구조**:
```sql
asm.service_requests     -- A/S 요청
  ├── sr_code
  ├── customer_id        -- 고객 참조
  ├── product_id         -- 제품 참조
  ├── serial_number      -- 제품 시리얼
  ├── purchase_date, warranty_end_date
  ├── issue_description
  ├── service_type       -- REPAIR/REPLACE/MAINTENANCE/INSPECTION
  ├── priority, status
  ├── assigned_technician_id
  ├── estimated_cost, actual_cost
  └── completed_at

asm.service_works        -- A/S 작업 내역
  ├── service_request_id
  ├── work_description
  ├── labor_hours
  └── labor_cost

asm.service_parts        -- A/S 부품 사용
  ├── service_request_id
  ├── part_id
  ├── quantity
  └── unit_cost
```

**용도**:
- 제품 수리/교체
- 정기 점검/유지보수
- 부품 교체 관리
- 비용 정산

---

### CRM (Customer Relationship Management) - 고객 관계 관리

**위치**: `03_crm.sql`

**테이블 구조**:
```sql
crm.customers            -- 고객/거래처 마스터
crm.customer_contacts    -- 고객 담당자
crm.customer_managers    -- 우리측 영업 담당
crm.customer_banks       -- 고객 계좌
crm.customer_addresses   -- 고객 주소
```

**용도**:
- 고객 마스터 관리
- 영업 관리
- 거래처 관계 관리

---

## 🔍 비교 분석

### 1. 도메인 책임 분석

| 도메인 | CSM | ASM | CRM |
|--------|-----|-----|-----|
| **핵심 목적** | 고객 지원/문의 | 제품 A/S | 고객 관계/영업 |
| **트리거** | 문의/불만 | 제품 고장 | 신규/기존 거래 |
| **프로세스** | 티켓 → 답변 → 완료 | 접수 → 수리 → 부품 → 완료 | 영업 활동 → 계약 |
| **결과물** | 답변/해결방안 | 수리 완료/부품 교체 | 거래 성사 |
| **비용 발생** | ❌ 무료 | ✅ 유료 (부품+인건비) | ❌ 영업 활동 |
| **제품 의존** | ❌ 선택적 | ✅ 필수 (시리얼 추적) | ❌ 무관 |
| **복잡도** | 낮음 | 높음 | 중간 |

### 2. 데이터 흐름 분석

```
┌─────────────────────────────────────────────────────────────┐
│                      고객 (Customer)                         │
└─────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┼───────────────┐
              │               │               │
              ▼               ▼               ▼
         ┌────────┐      ┌────────┐     ┌────────┐
         │  CRM   │      │  CSM   │     │  ASM   │
         │ 영업    │      │ 지원    │     │ A/S   │
         └────────┘      └────────┘     └────────┘
              │               │               │
              │               │               │
         고객 마스터      문의 티켓       수리 요청
         영업 활동       FAQ 조회        부품 교체
         계약 관리       만족도 조사      비용 정산
```

### 3. 업무 시나리오 비교

#### 시나리오 1: 제품 문의
```
고객: "이 제품 사용법이 궁금합니다"
→ CSM: support_tickets (기술 지원)
→ 답변 제공 → 완료
```

#### 시나리오 2: 제품 고장
```
고객: "제품이 작동하지 않습니다"
→ CSM: support_tickets (초기 접수)
→ ASM: service_requests (수리 접수)
→ 수리 작업 → 부품 교체 → 비용 청구
```

#### 시나리오 3: 영업 활동
```
영업팀: "신규 고객 발굴"
→ CRM: customers (고객 등록)
→ 영업 활동 → 계약 성사
```

---

## 💡 통합 시나리오 분석

### Option 1: CSM + ASM 통합 (After-Sales Service Management)

**새 이름**: `asm` (After-Sales Service Management)

```
asm (통합 후)
├── service_requests     -- A/S 요청 (기존)
├── service_works        -- A/S 작업 (기존)
├── service_parts        -- A/S 부품 (기존)
├── support_tickets      -- 고객 지원 티켓 (from csm) ⭐ NEW
├── ticket_comments      -- 티켓 댓글 (from csm) ⭐ NEW
└── faqs                 -- FAQ (from csm) ⭐ NEW
```

#### ✅ 장점
1. **고객 서비스 통합**: A/S + 지원을 한 곳에서 관리
2. **프로세스 연계**: 문의 → A/S 전환이 자연스러움
3. **담당자 일원화**: 동일 팀(고객 서비스팀)이 관리
4. **이력 통합**: 고객의 모든 서비스 이력 조회 가능

#### ❌ 단점
1. **도메인 혼재**: 단순 문의와 물리적 수리가 같은 스키마에
2. **복잡도 증가**: 이미 복잡한 asm에 테이블 추가
3. **역할 모호**: A/S 엔지니어와 고객지원 상담원의 역할이 다름

---

### Option 2: CSM + CRM 통합 (Customer Relationship Management)

**새 이름**: `crm` (Customer Relationship Management)

```
crm (통합 후)
├── customers            -- 고객 마스터 (기존)
├── customer_contacts    -- 고객 담당자 (기존)
├── customer_managers    -- 영업 담당자 (기존)
├── customer_banks       -- 고객 계좌 (기존)
├── customer_addresses   -- 고객 주소 (기존)
├── support_tickets      -- 고객 지원 티켓 (from csm) ⭐ NEW
├── ticket_comments      -- 티켓 댓글 (from csm) ⭐ NEW
└── faqs                 -- FAQ (from csm) ⭐ NEW
```

#### ✅ 장점
1. **고객 중심**: 모든 고객 접점을 CRM에서 관리
2. **영업-지원 연계**: 영업 과정의 문의를 자연스럽게 처리
3. **360도 뷰**: 고객의 모든 활동(영업, 문의, 계약)을 한눈에
4. **표준 구조**: Salesforce, HubSpot 등 CRM 솔루션과 유사

#### ❌ 단점
1. **역할 혼재**: 영업팀과 고객지원팀의 업무가 다름
2. **보안 이슈**: 영업 담당자가 모든 티켓을 볼 수 있음
3. **CRM 비대화**: 너무 많은 기능이 한 스키마에

---

### Option 3: CSM 독립 유지

**새 이름**: `07_csm.sql` (독립 스키마)

```
csm (독립)
├── support_tickets      -- 고객 지원 티켓
├── ticket_comments      -- 티켓 댓글
└── faqs                 -- FAQ
```

#### ✅ 장점
1. **책임 분리**: 각 도메인이 명확히 분리
2. **확장 용이**: 향후 챗봇, 지식베이스 추가 가능
3. **팀 독립성**: 고객지원팀의 독립적 운영
4. **보안**: 각 팀별 접근 권한 관리 용이

#### ❌ 단점
1. **스키마 증가**: 관리 대상 스키마 1개 증가
2. **중복 가능성**: asm과 일부 기능 중복
3. **복잡도**: 고객 서비스 이력이 분산됨

---

## 🎯 최종 권장안

### ⭐ **권장: Option 1 (CSM + ASM 통합)**

**이유**:

#### 1. **도메인 정합성**
```
CRM (영업) ─────┐
                ▼
            고객 마스터
                │
    ┌───────────┼───────────┐
    ▼                       ▼
ASM (고객 서비스)        기타 업무
├── 문의/지원            (구매, 판매 등)
├── A/S 수리
└── FAQ
```

- **CRM**: 영업 활동 (신규 고객 획득, 계약, 관계 관리)
- **ASM**: 사후 관리 (문의, A/S, 지원, 만족도)

#### 2. **실무 프로세스 일치**

실제 조직 구조:
```
영업팀 (Sales) → CRM
  └── 고객 획득, 계약

고객서비스팀 (Customer Service) → ASM
  ├── 헬프데스크 (문의 응대)
  └── A/S 센터 (수리/교체)
```

대부분 회사에서 **헬프데스크와 A/S 센터는 같은 부서**입니다.

#### 3. **티켓 → A/S 전환 시나리오**

```sql
-- 1단계: 고객이 "제품이 안 켜져요" 문의
INSERT INTO asm.support_tickets (
    subject = '제품 전원 문제',
    category = 'TECHNICAL'
);

-- 2단계: 상담원 판단 → A/S 필요
INSERT INTO asm.service_requests (
    linked_ticket_id = (support_ticket.id),  -- 연계
    service_type = 'REPAIR'
);

-- 3단계: A/S 완료 → 티켓 완료
UPDATE asm.support_tickets 
   SET status = 'RESOLVED',
       resolution = 'A/S 수리 완료'
```

이 흐름이 자연스럽게 한 스키마 안에서 처리됩니다.

#### 4. **통계 및 KPI 통합**

```sql
-- 고객 서비스 KPI를 한 번에 조회
SELECT 
    COUNT(*) FILTER (WHERE type = 'SUPPORT') AS 지원_티켓,
    COUNT(*) FILTER (WHERE type = 'AS') AS AS_건수,
    AVG(satisfaction_rating) AS 평균_만족도
FROM (
    SELECT 'SUPPORT' AS type, satisfaction_rating FROM asm.support_tickets
    UNION ALL
    SELECT 'AS', satisfaction_rating FROM asm.service_requests
) unified;
```

---

## 📝 구현 계획

### Step 1: ASM 스키마 확장

```sql
-- 13_asm.sql에 추가

-- 기존 테이블 (유지)
asm.service_requests
asm.service_works
asm.service_parts

-- 신규 테이블 (csm에서 이관)
asm.support_tickets      -- 지원 티켓 (일반 문의)
asm.ticket_comments      -- 티켓 댓글
asm.faqs                 -- FAQ
```

### Step 2: 테이블 수정

#### support_tickets 개선
```sql
-- linked_service_request_id 컬럼 추가
ALTER TABLE asm.support_tickets 
  ADD COLUMN linked_service_request_id UUID;

-- A/S 요청과 연계
ALTER TABLE asm.support_tickets 
  ADD CONSTRAINT fk_support_tickets__service_request_id
  FOREIGN KEY (linked_service_request_id) 
  REFERENCES asm.service_requests(id);
```

### Step 3: 스키마명 변경 고려

**Option A**: ASM 유지 (After-Sales Management)
- 기존 이름 유지
- A/S + 지원 모두 포함

**Option B**: CSS로 변경 (Customer Service & Support)
- 더 포괄적인 이름
- 지원 + A/S 포함 명확

**권장**: **ASM 유지** (기존 코드 호환성)

---

## 🔄 마이그레이션 계획

### Phase 1: 스키마 파일 생성 (1일)
```bash
# csm.sql.review 내용을 13_asm.sql에 통합
```

### Phase 2: 테이블 추가 (1일)
```sql
-- 13_asm.sql
CREATE TABLE asm.support_tickets ...
CREATE TABLE asm.ticket_comments ...
CREATE TABLE asm.faqs ...
```

### Phase 3: 외래키 추가 (1일)
```sql
ALTER TABLE asm.support_tickets 
  ADD COLUMN linked_service_request_id UUID;
```

### Phase 4: Backend 모델 생성 (2일)
```python
# apps/backend-api/src/api/models/tenant/asm/
support_ticket.py
ticket_comment.py
faq.py
```

### Phase 5: API 엔드포인트 추가 (2일)
```python
# apps/backend-api/src/api/routers/tnnt/asm.py
@router.get("/support-tickets")
@router.post("/support-tickets")
@router.get("/faqs")
```

**예상 소요 시간**: 1주일

---

## ✅ 최종 결론

### 📌 권장: **CSM을 ASM에 통합**

#### 통합 후 구조
```
13_asm.sql (After-Sales Management)
├── A/S 수리 관리
│   ├── service_requests
│   ├── service_works
│   └── service_parts
└── 고객 지원 관리 ⭐ NEW
    ├── support_tickets
    ├── ticket_comments
    └── faqs
```

#### 핵심 근거
1. ✅ **조직 구조 일치**: 고객서비스팀 = 헬프데스크 + A/S 센터
2. ✅ **프로세스 연계**: 문의 → A/S 전환 흐름이 자연스러움
3. ✅ **KPI 통합**: 고객 만족도, 처리 시간 등 통합 관리
4. ✅ **실무 효율**: 같은 팀이 사용하는 기능을 한 곳에

#### CRM과의 차이
- **CRM**: 고객 획득 및 영업 (사전 관리)
- **ASM**: 고객 유지 및 서비스 (사후 관리)

---

**다음 작업**: 13_asm.sql에 csm 테이블 통합 진행하시겠습니까?

---

**문서 끝**
