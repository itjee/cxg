# 테마 & 팔레트 설정 기능 구현

**날짜**: 2025-10-19 14:30:00 KST
**작성자**: Claude Code
**유형**: 기능 구현
**컴포넌트**: manager-web, tenants-web

## 개요

사용자가 시스템 설정에서 21개의 OKLCH 색상 팔레트 중 하나를 선택하여 전체 애플리케이션의 Primary 색상을 실시간으로 변경할 수 있는 고급 테마 설정 기능을 구현합니다.

## 기능 요구사항

### 1. 핵심 기능

- **색상 팔레트 선택**: 21개 색상 중 Primary 색상 선택
- **실시간 미리보기**: 색상 선택 시 즉시 UI 반영
- **다크/라이트 모드**: 각 모드별 최적화된 색상 자동 적용
- **로컬 저장**: 사용자 선호도 저장 (localStorage)
- **초기화**: 기본 테마로 복원 기능

### 2. 사용자 경험 (UX)

#### 2.1 비주얼 디자인
```
┌─────────────────────────────────────────────────────────┐
│  🎨 테마 설정                                            │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Primary 색상 선택                                       │
│  ┌──────────────────────────────────────────────────┐  │
│  │ [Violet] [Indigo] [Blue] [Sky] [Cyan] ...       │  │
│  │  ●        ○        ○      ○      ○               │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  미리보기                                                │
│  ┌──────────────────────────────────────────────────┐  │
│  │ [Button]  [Card]  [Input]  [Badge]               │  │
│  │  Primary  Secondary  Muted   Accent              │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  [기본값으로 초기화]              [적용]  [취소]        │
└─────────────────────────────────────────────────────────┘
```

#### 2.2 인터랙션 플로우
1. 시스템 설정 → 테마 탭 이동
2. 색상 팔레트 그리드 표시 (21개)
3. 색상 클릭 → 실시간 미리보기 업데이트
4. '적용' 버튼 → CSS 변수 업데이트 + 저장
5. 전체 UI 즉시 반영

### 3. 기술 구현

#### 3.1 아키텍처

```
┌─────────────────────────────────────────────────────┐
│              User Interface                          │
│  ┌──────────────┐  ┌──────────────┐                │
│  │ Color Picker │  │   Preview    │                │
│  │   Component  │  │   Component  │                │
│  └──────┬───────┘  └──────┬───────┘                │
│         │                  │                         │
├─────────┼──────────────────┼─────────────────────────┤
│         │   Theme Service  │                         │
│  ┌──────▼──────────────────▼───────┐                │
│  │  useTheme Hook                  │                │
│  │  - getTheme()                   │                │
│  │  - setTheme(palette)            │                │
│  │  - resetTheme()                 │                │
│  └──────┬──────────────────┬───────┘                │
│         │                  │                         │
├─────────┼──────────────────┼─────────────────────────┤
│         │   Storage Layer  │                         │
│  ┌──────▼──────┐  ┌────────▼───────┐               │
│  │ localStorage│  │  CSS Variables  │               │
│  │  'theme'    │  │  :root / .dark  │               │
│  └─────────────┘  └────────────────┘               │
└─────────────────────────────────────────────────────┘
```

#### 3.2 데이터 구조

```typescript
// 색상 팔레트 타입
type ColorPalette =
  | 'slate' | 'gray' | 'zinc' | 'neutral' | 'stone'
  | 'red' | 'orange' | 'amber' | 'yellow' | 'lime'
  | 'green' | 'emerald' | 'teal' | 'cyan' | 'sky'
  | 'blue' | 'indigo' | 'violet' | 'purple' | 'fuchsia'
  | 'pink' | 'rose';

// 테마 설정
interface ThemeConfig {
  palette: ColorPalette;
  mode: 'light' | 'dark';
  customColors?: {
    primary?: string;
    secondary?: string;
  };
}

// OKLCH 색상 데이터
interface ColorShades {
  50: string;
  100: string;
  200: string;
  300: string;
  400: string;
  500: string;
  600: string;
  700: string;
  800: string;
  900: string;
  950: string;
}
```

#### 3.3 CSS 변수 매핑

```css
/* 라이트 모드 */
:root {
  --primary-50: var(--{palette}-50);
  --primary-100: var(--{palette}-100);
  /* ... */
  --primary-600: var(--{palette}-600);  /* Primary 색상 */
  --primary: var(--primary-600);
}

/* 다크 모드 */
.dark {
  --primary-500: var(--{palette}-500);  /* 밝기 조정 */
  --primary: var(--primary-500);
}
```

## 구현 계획

### Phase 1: 기반 구조 (1-2시간)

1. **색상 상수 정의**
   - `lib/constants/colors.ts`: colorchip.html 데이터 변환
   - 모든 OKLCH 색상 값 export

2. **타입 정의**
   - `types/theme.types.ts`: ThemeConfig, ColorPalette 등

3. **테마 유틸리티**
   - `lib/theme/palette.ts`: 색상 변환 로직
   - `lib/theme/css-vars.ts`: CSS 변수 업데이트

### Phase 2: 상태 관리 (1시간)

1. **Zustand Store**
   ```typescript
   // stores/theme.store.ts
   interface ThemeState {
     palette: ColorPalette;
     mode: 'light' | 'dark';
     setPalette: (palette: ColorPalette) => void;
     toggleMode: () => void;
     resetTheme: () => void;
   }
   ```

2. **커스텀 Hook**
   ```typescript
   // hooks/use-theme.ts
   export function useTheme() {
     const { palette, setPalette, mode, toggleMode } = useThemeStore();

     useEffect(() => {
       // CSS 변수 업데이트
       updateCSSVariables(palette, mode);
     }, [palette, mode]);

     return { palette, setPalette, mode, toggleMode };
   }
   ```

### Phase 3: UI 컴포넌트 (2-3시간)

1. **ColorPalettePicker**
   ```typescript
   // components/theme/color-palette-picker.tsx
   - 21개 색상 그리드
   - 각 색상별 미니 칩 (50-950 샘플)
   - 선택 상태 표시
   - 호버 효과
   ```

2. **ThemePreview**
   ```typescript
   // components/theme/theme-preview.tsx
   - Button 미리보기
   - Card 미리보기
   - Input 미리보기
   - Badge 미리보기
   - 실시간 색상 반영
   ```

3. **ThemeSettings**
   ```typescript
   // components/theme/theme-settings.tsx
   - ColorPalettePicker
   - ThemePreview
   - 다크/라이트 토글
   - 저장/취소/초기화 버튼
   ```

### Phase 4: 시스템 설정 통합 (1시간)

1. **설정 페이지 생성**
   ```
   /app/(main)/cnfg/system/page.tsx
   - 탭 구조: 일반, 테마, 알림, 보안
   - 테마 탭에 ThemeSettings 컴포넌트
   ```

2. **네비게이션 추가**
   - Sidebar에 시스템 설정 메뉴 추가

### Phase 5: 테스트 & 문서화 (1시간)

1. **기능 테스트**
   - 21개 모든 팔레트 적용 확인
   - 다크/라이트 모드 전환 확인
   - localStorage 저장/로드 확인

2. **문서 작성**
   - 사용자 가이드
   - 개발자 문서 (색상 추가 방법)

## 고급 기능 (Optional)

### 1. 커스텀 색상 생성기

```typescript
// 사용자가 직접 OKLCH 색상 입력
interface CustomColorGenerator {
  lightness: number;  // 0-100
  chroma: number;     // 0-0.4
  hue: number;        // 0-360
}

// 자동으로 50-950 shade 생성
function generateColorShades(base: CustomColorGenerator): ColorShades {
  // 알고리즘: 명도 기반으로 11단계 생성
}
```

### 2. 테마 프리셋

```typescript
// 미리 정의된 테마 조합
const themePresets = {
  'ocean': { palette: 'cyan', accent: 'blue' },
  'forest': { palette: 'green', accent: 'emerald' },
  'sunset': { palette: 'orange', accent: 'rose' },
  'lavender': { palette: 'violet', accent: 'purple' },
  'minimal': { palette: 'neutral', accent: 'slate' },
};
```

### 3. 색상 접근성 검사

```typescript
// WCAG 대비율 검사
function checkColorContrast(
  foreground: string,
  background: string
): 'AAA' | 'AA' | 'Fail' {
  // OKLCH → sRGB → 명도 → 대비율 계산
}
```

### 4. 애니메이션 전환

```css
:root {
  transition:
    --primary 0.3s cubic-bezier(0.4, 0, 0.2, 1),
    --primary-foreground 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}
```

## UI/UX 디테일

### 1. 색상 팔레트 그리드

```typescript
<div className="grid grid-cols-7 gap-4">
  {palettes.map(palette => (
    <ColorPaletteCard
      key={palette.name}
      palette={palette}
      selected={currentPalette === palette.name}
      onClick={() => setPalette(palette.name)}
    >
      {/* 미니 그라데이션 프리뷰 */}
      <div className="flex h-12">
        <div style={{ background: palette['500'] }} className="flex-1" />
        <div style={{ background: palette['600'] }} className="flex-1" />
        <div style={{ background: palette['700'] }} className="flex-1" />
      </div>
      <p className="text-xs font-medium mt-2">{palette.label}</p>
    </ColorPaletteCard>
  ))}
</div>
```

### 2. 미리보기 컴포넌트

```typescript
<div className="space-y-4 p-6 bg-background rounded-lg border">
  <h3 className="text-lg font-semibold">미리보기</h3>

  {/* 버튼 샘플 */}
  <div className="flex gap-2">
    <Button>Primary</Button>
    <Button variant="secondary">Secondary</Button>
    <Button variant="outline">Outline</Button>
  </div>

  {/* 카드 샘플 */}
  <Card>
    <CardHeader>
      <CardTitle>Sample Card</CardTitle>
    </CardHeader>
    <CardContent>
      <p className="text-muted-foreground">
        This is how your theme looks
      </p>
    </CardContent>
  </Card>

  {/* 통계 카드 샘플 */}
  <StatsCards cards={sampleStats} columns={2} />
</div>
```

### 3. 반응형 레이아웃

```typescript
// 모바일
<div className="flex flex-col gap-6">
  <ColorPalettePicker />
  <ThemePreview />
</div>

// 데스크톱
<div className="grid grid-cols-2 gap-8">
  <ColorPalettePicker />
  <ThemePreview />
</div>
```

## 성능 최적화

### 1. CSS 변수 업데이트 최적화

```typescript
function updateCSSVariables(palette: ColorPalette, mode: 'light' | 'dark') {
  const root = document.documentElement;
  const colors = COLOR_PALETTES[palette];

  // Batch 업데이트
  requestAnimationFrame(() => {
    Object.entries(colors).forEach(([shade, value]) => {
      root.style.setProperty(`--primary-${shade}`, value);
    });

    // Primary 색상 설정
    const primaryShade = mode === 'light' ? '600' : '500';
    root.style.setProperty('--primary', `var(--primary-${primaryShade})`);
  });
}
```

### 2. 메모이제이션

```typescript
const MemoizedColorPaletteCard = memo(ColorPaletteCard);
const MemoizedThemePreview = memo(ThemePreview);
```

### 3. Debounce 적용

```typescript
const debouncedUpdateTheme = useMemo(
  () => debounce((palette: ColorPalette) => {
    updateCSSVariables(palette, mode);
    saveToStorage(palette);
  }, 150),
  [mode]
);
```

## 보안 고려사항

1. **XSS 방지**: CSS 변수 값 검증
2. **CSP 정책**: style-src 'unsafe-inline' 필요 여부 검토
3. **입력 검증**: 커스텀 색상 입력 시 OKLCH 형식 검증

## 브라우저 호환성

- **OKLCH 지원**: Chrome 111+, Safari 15.4+, Firefox 113+
- **Fallback**: 미지원 브라우저는 기본 색상 유지
- **Progressive Enhancement**: 기능 감지 후 활성화

## 테스트 시나리오

### 1. 기능 테스트
- [ ] 21개 팔레트 모두 적용 가능
- [ ] 라이트/다크 모드 전환 시 색상 자동 조정
- [ ] localStorage 저장/로드 정상 작동
- [ ] 초기화 버튼으로 기본 테마 복원

### 2. UI 테스트
- [ ] 반응형 레이아웃 정상 작동
- [ ] 색상 선택 시 즉시 미리보기 반영
- [ ] 애니메이션 부드럽게 작동
- [ ] 접근성: 키보드 네비게이션, ARIA 레이블

### 3. 성능 테스트
- [ ] CSS 변수 업데이트 < 16ms (60fps)
- [ ] 페이지 로드 시 깜빡임 없음
- [ ] 메모리 누수 없음

## 마일스톤

| Phase | 작업 내용 | 예상 시간 | 완료 |
|-------|----------|-----------|------|
| 1 | 기반 구조 (상수, 타입, 유틸) | 1-2h | ⬜ |
| 2 | 상태 관리 (Store, Hook) | 1h | ⬜ |
| 3 | UI 컴포넌트 구현 | 2-3h | ⬜ |
| 4 | 시스템 설정 통합 | 1h | ⬜ |
| 5 | 테스트 & 문서화 | 1h | ⬜ |
| **총계** | | **6-8h** | |

## 향후 개선사항

1. **AI 색상 추천**: 브랜드 이미지 기반 색상 제안
2. **테마 공유**: URL로 테마 공유 기능
3. **다중 팔레트**: Primary + Accent 조합
4. **그라데이션 생성**: 자동 그라데이션 팔레트
5. **색약 모드**: 색각 이상자를 위한 최적화

## 참고 자료

- [OKLCH Color Space](https://evilmartians.com/chronicles/oklch-in-css-why-quit-rgb-hsl)
- [Tailwind CSS Theming](https://tailwindcss.com/docs/customizing-colors)
- [Radix UI Colors](https://www.radix-ui.com/colors)
- [Material You Dynamic Color](https://m3.material.io/styles/color/dynamic-color/overview)
