# Tenants-Web 사이드바 메뉴 구성

**날짜**: 2025-10-26 19:55:30 KST
**작성자**: Claude Code
**유형**: UI 구현 | 네비게이션 개선
**컴포넌트**: tenants-web / Layout / Sidebar

## 개요

사이드바 메뉴를 데이터베이스 스키마 기반의 엔티티 구조에 맞춰 개편했습니다.

이제:
- 각 스키마별로 메뉴 그룹이 명확하게 구성됨
- 각 그룹 내에 세부 엔티티 메뉴 항목이 표시됨
- 설명(description)으로 각 그룹의 역할을 한눈에 파악 가능
- 확장/축약 상태에서 모두 직관적으로 사용 가능

## 메뉴 구조

### 메뉴 구성 원칙

```
Menu Group (13개)
  ├── name: 그룹 이름
  ├── icon: 그룹 아이콘 (lucide-react)
  ├── description: 그룹 설명 (각 스키마의 역할)
  └── items: 엔티티 메뉴 항목들
        ├── name: 메뉴 항목 이름
        ├── href: 라우트 경로
        └── icon: (선택) 아이콘
```

### 메뉴 그룹 목록

| 순서 | 그룹명 | 스키마 | 항목 수 | 설명 |
|------|--------|--------|---------|------|
| 1 | 대시보드 | - | 1개 | 홈 및 개요 |
| 2 | 기본 설정 | 01_adm | 7개 | 공통 코드, 환율, 단위 등 |
| 3 | 인사/급여 | 02_hrm | 9개 | 직원, 부서, 급여, 근태 관리 |
| 4 | 고객관계 | 03_crm | 5개 | 거래처, 영업기회, 활동 관리 |
| 5 | 제품 정보 | 04_pim | 5개 | 제품, 카테고리, 브랜드 관리 |
| 6 | 창고 관리 | 05_wms | 5개 | 창고, 수령, 배송, 재고 |
| 7 | 승인 관리 | 06_apm | 3개 | 승인 요청 및 이력 관리 |
| 8 | 재고 관리 | 10_ivm | 4개 | 재고 잔액, 이동, 조정, 실사 |
| 9 | 구매 관리 | 11_psm | 3개 | 구매요청, 주문, 견적 |
| 10 | 판매 관리 | 12_srm | 4개 | 판매주문, 배송, 송장 |
| 11 | 고객 지원 | 13_fsm | 3개 | 서비스요청, 티켓, FAQ |
| 12 | 재무 회계 | 14_fim | 3개 | 계정, 분개, 송장 |
| 13 | 고정 자산 | 15_fam | 2개 | 고정자산, 감가상각 |
| 14 | 워크플로우 | 16_lwm | 3개 | 워크플로우, 스텝, 업무 |
| 15 | 경영 분석 | 20_bim | 2개 | KPI, 분석 |
| 16 | 시스템 관리 | 22_sys | 3개 | 사용자, 역할, 권한 |

## 파일 구조

### 1. 메뉴 설정 파일

**파일**: `src/constants/menu-config.ts`

**목적**: 메뉴 구조 중앙 관리

**주요 내용**:
```typescript
// 타입 정의
export interface MenuItem {
  name: string;
  href: string;
  icon?: LucideIcon;
}

export interface MenuGroup {
  name: string;
  icon: LucideIcon;
  description?: string;
  items: MenuItem[];
}

// 메뉴 그룹 배열
export const menuGroups: MenuGroup[] = [
  {
    name: "대시보드",
    icon: Home,
    description: "홈 및 개요",
    items: [
      { name: "홈", href: "/overview", icon: Home },
    ],
  },
  // ... 다른 그룹들
];
```

### 2. 사이드바 컴포넌트

**파일**: `src/components/layout/sidebar.tsx`

**변경사항**:
- ❌ 하드코딩된 `moduleGroups` 제거
- ✅ 외부 `menu-config.ts`에서 `menuGroups` import
- ✅ 그룹 활성화 상태 감지 함수 추가
- ✅ 메뉴 항목 설명 표시 기능 추가

## 메뉴 기능

### 1. 그룹 확장/축약

```typescript
// 그룹 토글 함수
const toggleGroup = (groupName: string) => {
  setCollapsedGroups(prev => {
    const next = new Set(prev);
    if (next.has(groupName)) {
      next.delete(groupName);  // 확장
    } else {
      next.add(groupName);     // 축약
    }
    return next;
  });
};
```

### 2. 그룹 활성화 감지

```typescript
// 현재 경로가 그룹의 항목과 일치하는지 확인
const isGroupActive = (groupName: string) => {
  const group = menuGroups.find(g => g.name === groupName);
  if (!group) return false;
  return group.items.some(
    item => pathname === item.href ||
            pathname?.startsWith(item.href + "/")
  );
};
```

활성화된 그룹:
- 배경색이 인디고(indigo) 색으로 강조
- 아이콘과 텍스트 색 변경
- 유저가 현재 위치를 명확히 파악 가능

### 3. 메뉴 항목 하이라이트

```typescript
// 현재 경로와 메뉴 항목 경로 비교
const isItemActive =
  pathname === item.href ||
  pathname?.startsWith(item.href + "/");
```

활성 메뉴 항목:
- 배경: `indigo-100` (라이트 모드) / `indigo-900/70` (다크 모드)
- 텍스트: `indigo-700` (라이트 모드) / `indigo-200` (다크 모드)
- 그림자 효과 추가

## 레이아웃

### 확장된 상태 (w-64)

```
┌─────────────────────────────┐
│  [Logo] ConexGrow 워크스페이 │  ← 로고 및 타이틀
├─────────────────────────────┤
│                             │
│  📊 기본 설정               │  ← 그룹 헤더
│     공통 코드, 환율, 단위   │     (설명 표시)
│     - 코드 관리 ▶           │  ← 메뉴 항목
│     - 코드 그룹 ▶           │
│     - 설정 정보 ▶           │
│     ...                     │
│                             │
│  👥 인사/급여               │  ← 다음 그룹
│     직원, 부서, 급여 관리   │
│     - 부서 관리 ▶           │
│     ...                     │
│                             │
├─────────────────────────────┤
│  ❓ 도움말                  │  ← 도움말 버튼
├─────────────────────────────┤
│  👤 사용자                  │  ← 사용자 정보
│     user@company.com        │
│  🚪 로그아웃                │  ← 로그아웃 버튼
└─────────────────────────────┘
```

### 축약된 상태 (w-14)

```
┌──┐
│  │ ← 로고 아이콘
├──┤
│📊│ ← 그룹 아이콘 (툴팁 표시)
│👥│
│📦│
│...│
├──┤
│❓│
├──┤
│🚪│
└──┘
```

## 스타일링

### 색상 체계

| 상태 | 라이트 모드 | 다크 모드 |
|------|-----------|---------|
| **활성 그룹** | bg-indigo-50 | bg-indigo-900/50 |
| **활성 그룹 텍스트** | text-indigo-700 | text-indigo-300 |
| **활성 항목** | bg-indigo-100 | bg-indigo-900/70 |
| **활성 항목 텍스트** | text-indigo-700 | text-indigo-200 |
| **기본 텍스트** | text-gray-700 | text-gray-300 |
| **호버 배경** | hover:bg-gray-50 | dark:hover:bg-gray-700 |

### 간격 및 크기

- **그룹 간 간격**: `space-y-2`
- **항목 간 간격**: `space-y-0.5`
- **패딩**: 3 (md)
- **아이콘 크기**: h-4 w-4
- **폰트 크기**: sm

## 데이터 구조 예시

```typescript
const menuGroups: MenuGroup[] = [
  // 기본 설정 그룹
  {
    name: "기본 설정",
    icon: Settings,
    description: "공통 코드, 환율, 단위 등 기본 데이터",
    items: [
      { name: "코드 관리", href: "/adm/codes" },
      { name: "코드 그룹", href: "/adm/code-groups" },
      { name: "설정 정보", href: "/adm/settings" },
      { name: "통화 관리", href: "/adm/currencies" },
      { name: "환율 관리", href: "/adm/exchange-rates" },
      { name: "단위 관리", href: "/adm/units" },
      { name: "결제 조건", href: "/adm/payment-terms" },
    ],
  },
  // 인사/급여 그룹
  {
    name: "인사/급여",
    icon: Users,
    description: "직원, 부서, 급여, 근태 관리",
    items: [
      { name: "부서 관리", href: "/hrm/departments" },
      { name: "직원 관리", href: "/hrm/employees" },
      // ... 더 많은 항목
    ],
  },
  // ... 다른 그룹들
];
```

## 라우트 매핑

메뉴의 href는 `app/(main)/[schema]/[entity]/page.tsx` 구조와 일치합니다:

| 메뉴 항목 | 경로 | 페이지 |
|----------|------|-------|
| 코드 관리 | `/adm/codes` | `app/(main)/adm/codes/page.tsx` |
| 직원 관리 | `/hrm/employees` | `app/(main)/hrm/employees/page.tsx` |
| 거래처 관리 | `/crm/partners` | `app/(main)/crm/partners/page.tsx` |
| 제품 관리 | `/pim/products` | `app/(main)/pim/products/page.tsx` |

## 사용 방법

### 사이드바 컴포넌트 사용

```typescript
// layout.tsx에서
import { Sidebar } from "@/components/layout/sidebar";

export default function MainLayout({ children }) {
  const [collapsed, setCollapsed] = useState(false);
  const [mobileOpen, setMobileOpen] = useState(false);

  return (
    <div className="flex">
      <Sidebar
        collapsed={collapsed}
        mobileOpen={mobileOpen}
        onMobileClose={() => setMobileOpen(false)}
      />
      <main className="flex-1">
        {children}
      </main>
    </div>
  );
}
```

### 메뉴 항목 추가하기

1. `menu-config.ts` 파일 열기
2. 적절한 MenuGroup에 MenuItem 추가:

```typescript
{
  name: "새로운 기능",
  href: "/schema/entity",
}
```

또는 새 그룹 추가:

```typescript
{
  name: "새로운 그룹",
  icon: NewIcon,
  description: "설명",
  items: [
    { name: "항목 1", href: "/schema/entity1" },
    { name: "항목 2", href: "/schema/entity2" },
  ],
}
```

## 반응형 디자인

### 모바일 (< 1024px)

- 사이드바 기본 숨김
- 햄버거 메뉴로 토글
- 오버레이 배경 표시
- 상단에 닫기 버튼

### 데스크톱 (≥ 1024px)

- 사이드바 항상 표시
- 화면 왼쪽에 고정
- 메인 콘텐츠 옆에 배치

## 성능 최적화

### 동적 그룹 생성

```typescript
// 런타임에 menuGroups 기반으로 렌더링
{menuGroups.map((group) => (
  <div key={group.name}>
    {/* 그룹 렌더링 */}
  </div>
))}
```

### 상태 관리

- `collapsedGroups`: Set을 사용하여 O(1) 조회
- 불필요한 리렌더링 방지

## 향후 개선사항

### Phase 1 - 메뉴 개선

- [ ] 메뉴 검색 기능 추가
- [ ] 즐겨찾기 메뉴 기능
- [ ] 메뉴 기록 (최근 방문 항목)
- [ ] 단축키 지원

### Phase 2 - 개인화

- [ ] 메뉴 순서 커스터마이징
- [ ] 즐겨찾기 저장 (localStorage)
- [ ] 사용자별 권한에 따른 동적 메뉴
- [ ] 테마별 메뉴 아이콘

### Phase 3 - 고급 기능

- [ ] 메뉴 아이콘 동적 로딩
- [ ] 부패준 메뉴 (3단계 이상)
- [ ] 메뉴 드래그 드롭
- [ ] 메뉴 분석 및 최적화

## 주의사항

### 라우트 일관성

메뉴 경로(`href`)와 실제 페이지 경로가 일치해야 합니다:

```
menu-config.ts: "/adm/codes"
     ↓
app/(main)/adm/codes/page.tsx
```

### 설명 길이

`description` 필드는 사이드바 확장 상태에서 한 줄로 표시되므로, 너무 길지 않게 작성:

```typescript
// ❌ 너무 김
description: "이것은 매우 길고 복잡한 설명으로 여러 줄에 걸쳐 표시됩니다"

// ✅ 적절함
description: "공통 코드, 환율, 단위 등"
```

### 아이콘 선택

lucide-react의 아이콘을 사용하며, 의미있는 아이콘 선택:

```typescript
import {
  Home,          // 홈
  Users,         // 사용자/인사
  Package,       // 제품/재고
  ShoppingCart,  // 구매
  TrendingUp,    // 판매
  // ... 등등
} from "lucide-react";
```

## 관련 파일

- `src/constants/menu-config.ts` - 메뉴 설정
- `src/components/layout/sidebar.tsx` - 사이드바 컴포넌트
- `app/(main)/*/page.tsx` - 각 메뉴 페이지

