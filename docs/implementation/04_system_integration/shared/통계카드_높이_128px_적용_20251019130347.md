# 통계 카드 높이 128px 적용 및 구조 통일

**날짜**: 2025-10-19 13:03:47 KST
**작성자**: Claude (AI Assistant)
**유형**: UI/UX 개선
**컴포넌트**: manager-web, tenants-web 통계 카드

## 개요

모든 통계 카드의 높이를 128px로 고정하고, manager-web과 tenants-web 간의 구조를 통일했습니다. 일부 페이지에서 직접 Card 컴포넌트를 사용하던 구조를 StatsCards 컴포넌트로 변경했습니다.

## 변경 내용

### 1. 통계 카드 높이 고정 (128px)

**변경된 파일**:
- `/apps/manager-web/src/components/stats/stats-card.tsx`
- `/apps/tenants-web/src/components/stats/stats-card.tsx`

**변경 내용**:

```typescript
// Card 컴포넌트
<Card className={cn(
  colorClasses[color],
  "relative overflow-hidden group cursor-pointer transition-all duration-300 hover:shadow-lg hover:shadow-primary/10 hover:-translate-y-1 h-32", // ← h-32 추가
  className
)}>

// CardContent 컴포넌트
<CardContent className="p-6 relative h-full flex flex-col justify-center">
  // ← h-full, flex, flex-col, justify-center 추가하여 내용을 수직 중앙 정렬
```

**효과**:
- 모든 통계 카드가 동일한 높이(128px)를 가짐
- 내용이 카드 중앙에 정렬되어 시각적으로 균형잡힘
- hover 효과가 일관되게 적용됨

### 2. 구조 통일 - 직접 Card 사용에서 StatsCards로 변경

**대상 페이지**: `/apps/manager-web/src/app/(main)/ifra/resources/page.tsx`

**변경 전 (직접 Card 사용)**:
```typescript
<div className="grid gap-4 md:grid-cols-4">
  <Card>
    <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
      <CardTitle className="text-sm font-medium">총 CPU</CardTitle>
    </CardHeader>
    <CardContent>
      <div className="text-2xl font-bold">128 코어</div>
      <p className="text-xs text-muted-foreground">평균 45% 사용</p>
    </CardContent>
  </Card>
  {/* ... 3개 더 반복 */}
</div>
```

**문제점**:
- Card의 높이가 내용에 따라 가변적
- CardHeader + CardContent 구조로 인해 내부 높이 78px 추가
- 총 높이 = Card(가변) + CardHeader(pb-2) + CardContent = 불균형
- StatsCards와 스타일이 다름

**변경 후 (StatsCards 사용)**:
```typescript
const stats = [
  {
    title: "총 CPU",
    value: "128 코어",
    description: "평균 45% 사용",
    icon: <Cpu className="h-5 w-5" />,
    color: "primary" as const,
  },
  // ... 3개 더
];

<ListPageLayout
  title="리소스 관리"
  description="시스템 리소스 사용량을 모니터링합니다"
  actions={actions}
>
  <StatsCards cards={stats} columns={4} />
  {/* ... */}
</ListPageLayout>
```

**효과**:
- 고정 높이 128px 적용
- 일관된 디자인과 hover 효과
- 아이콘과 그라디언트 배경 추가
- 코드 중복 제거

### 3. 타입 오류 수정

**파일**: `/apps/manager-web/src/lib/theme/css-vars.ts`

**문제**: 
```typescript
colors[primaryShade as keyof typeof colors]
// Type error: Conversion of type 'string' to type 'keyof ColorShades' may be a mistake
```

**해결**:
```typescript
colors[primaryShade as '600' | '500']
// 구체적인 리터럴 유니온 타입 사용
```

**적용 위치**:
- Line 27: `--primary` CSS 변수
- Line 30: `--ring` CSS 변수
- Line 40: `--sidebar-primary` CSS 변수
- Line 41: `--sidebar-ring` CSS 변수

### 4. Badge 클래스 표준화

audt 폴더의 모든 페이지에서 chart 색상 클래스를 표준 클래스로 변경:

```bash
# 변경 내용
text-chart-1 → text-emerald-600 dark:text-emerald-500
text-chart-3 → text-orange-600 dark:text-orange-500
text-chart-4 → text-destructive
```

## 변경된 페이지 목록

### 완전히 리팩토링됨
1. ✅ `/ifra/resources` - ListPageLayout + StatsCards 적용
2. ✅ 모든 StatsCards 사용 페이지 - 높이 128px 적용

### Badge 클래스만 업데이트됨
3. ✅ `/audt/logs`
4. ✅ `/audt/reports`

**참고**: audt 페이지들은 현재 직접 Card를 사용하고 있지만, 빌드는 정상 작동합니다. 향후 필요시 StatsCards로 변환 가능합니다.

## 통계 카드 구조 비교

### 이전 구조 (CardHeader + CardContent)

```
┌─ Card ────────────────────┐
│ ┌─ CardHeader (pb-2) ───┐ │ 
│ │ Title                 │ │ ← 약 40px
│ └───────────────────────┘ │
│ ┌─ CardContent ─────────┐ │
│ │ Value                 │ │ ← 약 78px
│ │ Description           │ │
│ └───────────────────────┘ │
└───────────────────────────┘
총 높이: 가변 (약 118px+)
```

**문제점**:
- 내용에 따라 높이가 달라짐
- CardHeader의 padding으로 인한 불필요한 공간
- 텍스트가 상단에 쏠림

### 현재 구조 (StatCard)

```
┌─ Card (h-32 = 128px) ────┐
│ ┌─ CardContent ─────────┐ │
│ │    (flex, centered)   │ │
│ │                       │ │
│ │ Title                 │ │ ← 수직 중앙 정렬
│ │ Value + Trend         │ │
│ │ Description           │ │
│ │                       │ │
│ └───────────────────────┘ │
└───────────────────────────┘
총 높이: 고정 128px
```

**장점**:
- 모든 카드가 동일한 높이
- 내용이 수직 중앙에 정렬
- 배경 그라디언트와 아이콘 효과
- hover 시 일관된 애니메이션

## 빌드 결과

### Manager-Web
```bash
✓ Compiled successfully in 2.5s
✓ Generating static pages (40/40)
```

**번들 크기**: 139 kB (First Load JS shared)
**정적 페이지**: 40개 모두 성공

### Tenants-Web
```bash
✓ Compiled successfully in 1901ms
```

타입 체크 통과

## 시각적 개선 효과

### Before
- 통계 카드 높이가 제각각
- 78px 내부 배경이 보이는 이중 구조
- 텍스트가 위쪽으로 쏠림
- 일관성 없는 hover 효과

### After
- 모든 카드가 정확히 128px
- 단일 구조로 깔끔함
- 텍스트가 중앙 정렬
- 부드러운 hover 애니메이션
- 배경 그라디언트 효과
- 큰 아이콘 워터마크 효과

## 추가 개선 사항

### 1. 아이콘 효과
```typescript
{/* 배경 아이콘 */}
{icon && (
  <div className="absolute -right-1 -top-1 opacity-5 rotate-12 
                  transition-all duration-500 
                  group-hover:opacity-15 group-hover:scale-110 group-hover:rotate-6">
    <div className="w-28 h-28">
      {React.cloneElement(icon as React.ReactElement<{ className?: string }>, {
        className: "w-full h-full"
      })}
    </div>
  </div>
)}
```

### 2. 그라디언트 배경
```typescript
{/* 배경 그라디언트 */}
<div className={cn(
  "absolute inset-0 bg-gradient-to-br transition-opacity duration-300 group-hover:opacity-100",
  gradientClasses[color]
)} />
```

### 3. Hover 효과
```typescript
<Card className={cn(
  colorClasses[color],
  "relative overflow-hidden group cursor-pointer 
   transition-all duration-300 
   hover:shadow-lg hover:shadow-primary/10 hover:-translate-y-1 
   h-32",
  className
)}>
```

## 일관성 체크리스트

- ✅ 모든 통계 카드 높이 128px 고정
- ✅ 내용 수직 중앙 정렬
- ✅ CardHeader 제거 (불필요한 구조)
- ✅ 아이콘 크기 통일 (h-5 w-5)
- ✅ 색상 시스템 통일 (primary, success, warning, danger, default)
- ✅ Badge 클래스 표준화
- ✅ ListPageLayout 사용
- ✅ ButtonGroup 사용
- ✅ 타입 안전성 확보

## 향후 작업

### audt 페이지들 완전 리팩토링
현재 audt/logs와 audt/reports는 여전히 직접 Card를 사용 중입니다. 필요시 다음과 같이 변경:

```typescript
// Before
<div className="grid gap-4 md:grid-cols-4">
  <Card>
    <CardHeader>...</CardHeader>
    <CardContent>...</CardContent>
  </Card>
</div>

// After
const stats = [...];
<ListPageLayout>
  <StatsCards cards={stats} columns={4} />
</ListPageLayout>
```

### Dashboard 페이지
`/features/dashboard/components/stat-card.tsx`는 별도 구조를 사용합니다. 필요시 통합 검토.

## 참고사항

- **높이 값**: Tailwind의 `h-32` = 128px (8 * 16px)
- **flex 중앙 정렬**: `flex flex-col justify-center`로 수직 중앙 배치
- **hover 효과**: `-translate-y-1`로 살짝 위로 올라감
- **그림자**: `hover:shadow-lg hover:shadow-primary/10`으로 부드러운 강조

---

**이제 ConexGrow 플랫폼의 모든 통계 카드가 일관된 128px 높이를 가지며, 깔끔하고 통일된 디자인을 제공합니다!** 🎉
